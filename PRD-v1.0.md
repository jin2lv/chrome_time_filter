# 时光机（TimeMachine）— 产品需求文档 v1.0

> **一句话定义**：浏览器插件，让用户在社交媒体/社区论坛上只看到指定时间点之前的帖子，过滤掉其后的内容。
>
> **代号**：TimeMachine
> **版本**：PRD v1.0（2026-08-10，2026-08-14 修订，2026-09-17 校正实现状态）

---

## 1. 背景与目标

### 1.1 问题陈述

社交媒体和社区论坛的信息流天然偏好"最新最热"内容。这带来三个痛点：

1. **投资决策噪音**：股票社区（雪球、股吧、同花顺）盘中实时帖子 90% 是情绪噪音，严重影响决策冷静
2. **信息可信度无法验证**：暴跌之后满屏"我早就说了"，无法区分事前判断和事后诸葛亮
3. **剧透污染**：新剧/综艺播出后，讨论区被剧透淹没，想参与讨论又不想被剧透

现有浏览器插件要么只做单平台时间过滤（如 Reddit Time Filter，2022年停更），要么做关键词/语义过滤（如 NoJunkFeed、Fix My Feed），**没有一款做"跨平台时间截止/区间过滤"**。

### 1.2 产品目标

| 目标 | 衡量标准 |
|------|---------|
| 按时间过滤帖子 | 设定截止时间后，该时间后的帖子不显示 |
| 支持多平台 | v1.0 聚焦国内金融网页端；先完成雪球全场景，再扩展同花顺、东方财富、东方财富股吧、淘股吧、天天基金、集思录（通达信经取证无社区，不纳入） |
| 低维护成本 | 平台改版时只需更新 JSON 选择器配置，不改架构 |
| 轻量无自建后端 | 纯客户端运行，不收集数据；适配包更新走公开静态托管 |

### 1.3 非目标（明确不做）

- ❌ 定期快照/历史保存
- ❌ 回放模式
- ❌ AI 语义分析/情绪打分（v2.0 按 BYOK 方案探索）
- ❌ 后端服务/账号体系/数据同步
- ❌ 移动端原生 App 适配
- ❌ 微博适配（2026-08-12 决议：关注流仅保留约 10 页，深度硬限制无法突破）

---

## 2. 目标用户

| 用户画像 | 核心需求 | 使用频率 |
|----------|---------|---------|
| **个人投资者**（主） | 盘中隔离噪音、事前/事后情绪切割 | 高频（几乎每个交易日） |
| **影视/综艺爱好者**（次） | 剧透防火墙——讨论但不被剧透 | 中频（追剧季密集，平时低） |

---

## 3. 核心概念

### 3.1 时间模式

| 模式 | 说明 | 版本 |
|------|------|------|
| **截止模式** | 只显示发帖时间 ≤ 截止时间 T 的帖子 | v1.0 |
| **窗口模式** | 只显示 T1 ≤ 发帖时间 ≤ T2 的帖子 | v1.0 |

### 3.2 过滤策略

| 策略 | 表现 | 版本 |
|------|------|------|
| **隐藏** | `display: none`，不占位 | v1.0 |
| **折叠** | 占位条"⏳ 此帖被时光机过滤"，点击可展开 | v1.0 |
| **模糊** | `filter: blur(8px)`，点击临时解除 | v1.2 |

### 3.3 过滤计数器

```
⏳ 时光机已激活 | 截止: 2026-08-09 15:00 | 已过滤 47 条
```

展示位置：插件图标角标 + 控制面板内文字 + 可选悬浮提示条（右下角，可关闭）。

### 3.4 多 Tab 状态模型

| 状态维度 | 作用域 | 行为 |
|----------|--------|------|
| 时间设置 | **按域名全局同步** | 同域名所有 Tab 共享，通过 `storage.onChanged` 同步 |
| 过滤开关 | **按 Tab 独立** | 新 Tab 默认开启；该域名从未设定时间时开关不可用 |
| 过滤策略 | 按域名 + 可覆盖 | 全局默认策略，可按平台覆盖 |

### 3.5 评论的时间归属

- **评论有时间戳**：评论按自身时间戳独立参与过滤。旧帖子的新评论会被隐藏/折叠，保持时间隔离感。
- **评论无时间戳**：回退到用户在设置中指定的策略——"全部显示"或"默认折叠"。

---

## 4. 功能需求

### F1：时间设置（P0）

点击插件图标弹出控制面板（v1.0 支持截止模式与时间区间模式）：

```
┌─────────────────────────────────┐
│  ⏳ 时光机               [开启] │
│                                 │
│  截止时间:  [2026-08-09 15:00]  │
│  [快捷: 1小时前 | 今日0点 |      │
│   昨日0点 | 上周一 | 自定义]     │
│                                 │
│  过滤策略: (●)隐藏  ( )折叠      │
│                                 │
│  当前站点: 雪球 xueqiu.com       │
│  已过滤: 47 条                   │
└─────────────────────────────────┘
```

**规则**：
- 时间精度到分钟，设定后立即生效
- **域名首次使用必须先设定时间**：从未设定时间的域名，过滤开关禁用。杜绝"开着一个没有 T 的过滤器"的歧义状态
- 时间设置按域名记忆；截止模式提供“1小时前/今日0点/昨日0点”及金融预设（今天09:15/今天09:30/昨日收盘前），区间模式提供“今天/昨天/近3天/本周”及金融预设（午间复盘/交易时段/交易日全天）。金融预设 v1.0 已落地，“最近一个交易日”按工作日口径处理（法定节假日留待 v1.1 交易日历）

### F2：帖子过滤执行（P0）

**过滤范围（v1.0）**：

| 页面类型 | 是否过滤 |
|---------|---------|
| 信息流/列表页 | ✅ |
| 帖子详情页评论 | ✅（评论按自身时间戳独立判定） |
| 搜索结果页 | ❌ |
| 消息通知/私信 | ❌ |
| 个人主页 | ❌ |

**时间戳解析策略**：

| 时间类型 | 处理方式 |
|----------|---------|
| 绝对时间 | 直接解析 |
| 相对时间（"3小时前"、"昨天"） | **时间锚定**：以首次检测到该帖子元素的时刻为锚点反向推算，精度分钟级 |
| 无法解析 | 默认显示 + 计数器旁标注 "N 条无法解析" |
| 组合时间（同花顺 MMDD+HH:mm） | date_attr + text 组合解析，缺年份补当年，未来时间回退一年 |

**异常处理**：
- 平台改版选择器失效 → **不过滤（全部显示）** + 强制模态提示。宁可噪音回来，也不能让用户误以为"没有新帖子"
- 相对时间平台 UI 标注 "⚠️ 精度 ±5 分钟"

### F3：平台适配管理（P0）

每个目标平台一个 JSON "适配包"，包含帖子选择器、时间戳选择器与解析规则。**适配包是数据而非代码**，好处：
- 平台改版时只需更新配置，可热更新
- 高级用户可自行编写/导入适配包

**v1.0 适配计划**：

| 平台 | 域名 | 优先级 | 状态 |
|------|------|--------|------|
| 雪球 | xueqiu.com | P0 | ✅ 个股页、首页类别信息流、截止/区间模式、虚拟分页与信息流补拉、详情评论均已实现并通过真机验收（2026-09-09 验收闸门关闭） |
| 同花顺 | 10jqka.com.cn | P0 | 🚧 已完成 `t.10jqka.com.cn` 基础适配；其他目标页面待取证 |
| 东方财富 | eastmoney.com | P0 | 🚧 内置适配包 `eastmoney-news.json` 已落地（`finance.eastmoney.com` `/a/` 栏目列表）；其余页面类型与真机验收待补 |
| 东方财富股吧 | guba.eastmoney.com | P0 | 🔲 待适配；与淘股吧不是同一网站 |
| 淘股吧 | taoguba.com.cn | P0 | 🔲 待适配 |
| 天天基金 | fund.eastmoney.com 等 | P0 | 🔲 待适配；需区分基金详情、基金吧及复用东方财富结构的页面 |
| 集思录 | jisilu.cn | P0 | 🚧 内置适配包 `jisilu.json` 已落地（分类列表 + 详情回复）；真机验收待补 |
| 通达信网页版 | tdx.com.cn | 不承诺 | ❌ 2026-08-18 取证：`www.tdx.com.cn` 为软件产品站、`sns.tdx.com.cn` 为投教内容，均无公共 UGC 信息流，无可适配社区（见 `docs/platform-recon.md` §7），不纳入发布承诺 |
| ~~微博~~ | ~~weibo.com~~ | ~~已放弃~~ | **深度硬限制**（关注流仅约 10 页），2026-08-12 决议放弃 |

**适配口径**：对外支持的最小单位是“页面类型”，不是品牌。每个平台必须列出已验证的列表、详情评论、分页/加载方式、排序方式、类别上下文和时间精度；只完成一个页面不得笼统宣称支持整个网站。

### F4：快捷开关（P0）

- 插件图标点击 = 打开控制面板（面板顶部为开/关按钮）
- 快捷键：`Alt+Shift+T`（可自定义。原 `Ctrl+Shift+T` 与 Chrome 内置「重新打开已关闭标签页」冲突，2026-09-08 更换；suggested_key 变更后需在 `chrome://extensions/shortcuts` 手动绑定一次）
- 开启时图标有色+角标，关闭时灰色

### F5：过滤计数器（P1）

- 插件图标角标 + 控制面板内文字 + 可选悬浮提示条
- MutationObserver 检测新帖过滤 → 计数 +1；切时间 → 重置；页面导航/刷新 → 重置

### F6：设置页（P1）

| 分类 | 配置项 | 默认值 |
|------|--------|--------|
| 通用 | 默认过滤策略 | 隐藏 |
| 通用 | 角标显示计数 | 开 |
| 通用 | 悬浮提示条 | 关 |
| 评论过滤 | 评论无时间戳回退策略 | 全部显示 |
| 站点与适配 | 已授权站点管理、单站授权/撤销、每站时间设置记忆与重置 | — |
| 站点与适配 | 适配包版本信息、平台/页面类型能力矩阵 | — |
| 高级 | 适配包自动更新 | 开（每 12h 检查） |
| 高级 | 自定义适配包导入 | 规划中（P3） |

设置页可从 Popup 标题栏的设置按钮直接进入（`chrome.runtime.openOptionsPage()`）；桌面端按“通用/评论过滤/站点与适配/高级”分区，窄屏使用顶部分类导航。扩展版本号显示在设置页标题副标题，不单独设“关于”分区。

### F7：雪球个股页虚拟分页（P0）

雪球个股讨论列表启用过滤后，由时光机接管列表展示并将跨原生页的结果重新分页：

```
从雪球原始第 1 页开始 → 解析并过滤 → 去重收集符合条件的帖子
  → 收满 10 条即渲染「时光机第 1 页」并停止扫描
  → 用户点击「下一页」时，从缓存游标继续扫描直至再收满 10 条
```

**规则**：
- 懒加载而非预取：不预先扫描 200 页，每个虚拟页一旦收满 10 条立即停止。
- `200` 是一次过滤会话的原生页扫描硬上限；到达末页、上限或加载失败时必须明确提示。
- 使用 `a.date-and-source[data-id]` 作为稳定帖子 ID 去重；无法解析时间的帖子默认显示并计数。
- 虚拟列表克隆展示 DOM，保留原帖链接；不承诺克隆内点赞、评论等雪球原生交互可用。
- 原生列表和分页在虚拟模式下隐藏；修改截止时间、关闭过滤或页面重载时取消旧扫描并干净恢复。
- 提供上一页、下一页、加载中和取消扫描控制；上一页使用本地缓存，不重新请求。
- 该能力只在雪球适配包声明的个股列表结构命中时启用，雪球首页、详情评论和其他平台继续使用普通 DOM 过滤。
- 个股页“全部/讨论/投资者关系/交易/资讯/公告”六个类别使用独立虚拟分页会话；切换类别时取消旧扫描、清空旧类别缓存并从新类别第 1 页重新聚合。

### F8：类别信息流与平台能力透明（P0）

雪球首页的一级信息流“关注/热门/7x24/自选/视频/基金/资讯/达人/私募/ETF”，关注流下的“全部/关注精选/只看原发/未分组”，以及自选流下的“全部/公告/新闻”，均视为独立信息流上下文。切换类别时必须：

- 取消旧上下文中的扫描、观察器和异步任务；
- 隔离缓存、游标、去重集合、过滤数和无法解析数；
- 重新识别新列表、排序方式、加载机制与时间戳；
- 继续应用当前截止或起止时间设置；
- 对智能/热度排序明确标注“仅过滤已加载内容”或“结果可能不完整”，不得暗示已经找全时间范围内的帖子。

雪球首页登录态 DOM 基线（2026-08-14）：普通类别使用 `article.timeline__item` 与 `a.date-and-source[data-id]`；7x24 使用 `.timeline__live tr`，将 `.home__timeline-live__hd` 日期分组标题与首列 `HH:mm` 组合解析。普通类别分析字段为 `order=smart`，因此当前只承诺过滤已加载内容。

其他金融网站遵循同一规则：凡页面存在资讯/讨论/公告/基金/自选等类别或二级筛选，均按独立上下文取证和验收。类别选择器、稳定 ID、列表、分页/加载方式与排序能力放入平台适配配置，通用引擎不得硬编码站点名称。

设置页提供平台能力矩阵，至少展示：已验证页面类型、列表过滤、评论过滤、区间模式、跨页聚合能力、时间精度、排序完整性、适配包版本和最后验证日期。

无法解析诊断必须允许用户查看平台、页面类型、当前类别、原始时间文本和采用的回退行为，并复制脱敏报告。诊断默认不包含帖子正文、账号、Cookie 或其他登录数据，不上传任何内容。

多站点授权仍坚持按需原则。设置页可提供用户主动触发的“授权全部金融站点”，但安装时不得静默申请全部网站权限；同时支持单站授权、撤销和每站默认时间模式/策略/预设记忆。

### F9：v1.0 开发顺序闸门（P0）

v1.0 严格按以下顺序推进：

1. 完成雪球首页及个股/详情场景、全部类别上下文、截止/区间模式、虚拟分页或补拉、诊断与真机验收；
2. 抽象并稳定类别上下文、通用补拉、能力矩阵和适配健康检查；
3. 抽象通用虚拟分页/补拉，完成扫描可信状态、平台能力矩阵、金融预设、完整诊断、适配健康与多站授权体验；
4. 再依次适配东方财富股吧、淘股吧、天天基金/东方财富、同花顺扩展页面、集思录；
5. 通达信网页版经 2026-08-18 取证无公共 UGC 信息流，已不纳入发布承诺（见 §4 F3、`docs/platform-recon.md` §7）。

雪球完整验收未通过前，不并行启动其他平台的正式适配开发。

---

## 5. 交互流程

### 5.1 首次使用

```
安装插件 → 自动打开引导页（3步）
  Step 1: 说明时间截止概念
  Step 2: 选择默认过滤策略
  Step 3: 提示在雪球上试用
→ 引导完成后，插件默认关闭，等用户主动开启
```

### 5.2 日常使用（投资场景）

```
开盘前打开雪球 → 点插件图标 → 选"昨日0点" → 开启
  → 盘中刷雪球，只看到昨天及之前的帖子
  → 盘后想看今日讨论 → 关闭 → 帖子恢复
```

---

## 6. 技术架构

### 6.1 架构概览

```
┌──────────────────────────────────────────┐
│            Browser Extension             │
│  ┌──────────┐   ┌──────────────────────┐ │
│  │ Popup    │   │ Background (SW)      │ │
│  │ 控制面板  │◄─►│ 状态管理/跨Tab同步    │ │
│  └──────────┘   └──────────────────────┘ │
│  ┌────────────────────────────────────┐  │
│  │ Content Script (注入目标页面)       │  │
│  │  MutationObserver + AdapterManager │  │
│  │  时间解析 → 过滤判定 → DOM 操作    │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
```

数据流：用户设定时间 T → SW 存储 → Content Script 读取 → MutationObserver 检测新帖 → Adapter 解析时间戳 → 过滤 + 计数。

### 6.2 技术选型

| 组件 | 选型 |
|------|------|
| 扩展规范 | Manifest V3 |
| 构建工具 | Vite 8 + CRXJS 2.7 |
| UI 框架 | Vanilla CSS（Popup/Options 原生） |
| 状态管理 | chrome.storage.local |
| 时间解析 | dayjs 1.11 |
| 权限模型 | optional_host_permissions（按需授权） |

### 6.3 适配包热更新

```
SW 定时器（每 12h）+ 安装时首拉
  → 从 GitHub Releases 拉取最新适配包 JSON（jsDelivr CDN）
  → 成功：更新存储 + 广播 ADAPTERS_UPDATED
  → 失败：静默降级，使用内置兜底版本
```

拉取不携带用户标识，用户可在设置页关闭自动更新。

### 6.4 信息流机制与前端过滤前提

论坛/社区类网站的历史内容获取几乎全部是"前端主动请求 + 游标分页"——翻页能翻到任意早的内容。平台信息流机制分四类（页面级，非平台级）：

| 类型 | 代表页面 | 对"拿到 T 前内容"的影响 |
|------|---------|------------------------|
| **A. 时间倒序 + 游标分页** | 雪球个股区、贴吧楼层、虎扑、股吧、豆瓣 | ✅ 理论上无限回溯 |
| **B. 智能/热度排序** | 雪球首页、知乎默认排序 | ⚠️ 翻页拿到的非严格时间序 |
| **C. 排序可切换** | 知乎（切"按时间"）、贴吧（正序/倒序） | ✅ 切换后获得严格时间序 |
| **D. 深度有限制** | 微博关注流（约 10 页）、B站动态流 | ❌ 硬天花板，放弃适配 |

**核心风险与当前处理**：设昨日 0 点时，雪球个股区原始前几页可能全部被过滤。v1.0 已用配置驱动的虚拟分页解决个股区首屏空白：按需翻原生页并把符合条件的帖子重新组成每页 10 条。雪球首页不同类别可能采用智能/热度排序、时间排序或不同加载机制，v1.0 逐类取证：能可靠回溯的类别接入通用补拉，不能保证完整性的类别只过滤已加载内容并明确提示。

### 6.5 深度硬限制平台决议

| 平台 | 硬限制证据 | 决议 |
|------|-----------|------|
| **微博** | 关注流默认仅最近约 10 页 | ❌ 放弃适配 |
| **B站动态流** | "实时信息流"，无历史检索 | ❌ 放弃动态流 |
| **B站评论区** | 无深度限制 | ✅ 保留（剧透场景） |

---

## 7. 数据与隐私

| 项目 | 策略 |
|------|------|
| 用户数据 | 不收集、不上传、不分析 |
| 数据存储 | 仅 chrome.storage.local |
| 权限请求 | optional_host_permissions 按需授权 |
| 适配包更新 | 每 12h 从公开静态托管拉取，不携带用户标识 |
| 第三方服务 | 无 |
| 隐私声明 | 详见 `docs/PRIVACY-POLICY.md` |

---

## 8. 关键设计决策

以下决策来自开发过程中的讨论与裁决：

1. **图标点击 = 打开面板**（而非快速开关）。开关在面板顶部，快速开关走快捷键。（2026-08-10 裁决）
2. **"新 Tab 默认开启"与"安装后默认关闭"自洽**：未设定时间的域名开关不可用，两个默认态由此统一。（2026-08-10 裁决）
3. **无后端 + 适配包热更新可共存**：热更新走公开静态托管（GitHub Releases + jsDelivr），无自建服务端点。（2026-08-10 裁决）
4. **微博放弃适配**：关注流仅保留最近约 10 页（深度硬限制），核心场景"首页时间线过滤"无法成立。（2026-08-12 裁决）
5. **B站保留评论区适配，放弃动态流**：评论区无深度限制，剧透场景实际发生在视频评论区。（2026-08-12 裁决）
6. **雪球个股虚拟分页前移到 v1.0**：简单“空页跳到下一原生页”无法把分散的符合条件帖子重排到前几页；改为按虚拟页懒扫描、缓存和稳定 ID 去重。（2026-08-13 裁决）
7. **v1.0 聚焦金融并扩大覆盖**：区间模式、通用补拉/排序透明度、类别上下文和金融平台适配均前移；但必须先完成雪球全场景验收，再启动其他平台适配。（2026-08-13 裁决）
8. **“股吧”统一写为“东方财富股吧”**：`guba.eastmoney.com` 与淘股吧 `taoguba.com.cn` 是两个独立网站，分别适配和验收。（2026-08-13 裁决）

---

## 9. 路线图

| 版本 | 目标 | 关键功能 |
|------|------|---------|
| **v1.0** | 金融首发 | 雪球全场景优先；截止+区间模式、类别信息流、通用补拉/能力透明；再覆盖同花顺、东方财富、东方财富股吧、淘股吧、天天基金、集思录（通达信经取证无社区，不纳入） |
| **v1.1** | 金融体验增强 | 快捷键、设置导入导出、交易日历与适配维护工具 |
| **v1.2** | 剧透线 | 豆瓣适配 + 模糊策略 |
| **v2.0** | 场景扩展 | 体育录像党（虎扑/懂球帝）、增量续读、合订本、BYOK 语义过滤 |

v1.1+ 的场景验证、平台扩展优先级与 BYOK 方案见 §12。

---

## 10. 风险与对策

| 风险 | 等级 | 对策 |
|------|------|------|
| 平台改版选择器失效 | **高** | JSON 适配包 + 12h 热更新 + 失效时不过滤 + 强制模态提示 |
| 首屏全空（T 过于激进） | **高** | 雪球个股页使用虚拟分页按需聚合；v1.0 在其他平台适配前完成通用补拉/排序能力 |
| 自动扫描触发风控或加载失败 | **高** | 串行懒扫描、收满即停、200 原生页硬上限、可取消、重复页/超时明确报错 |
| 用户量天花板低 | 中 | 聚焦投资垂直场景做深，免费降低门槛 |
| SW 生命周期限制 | 低 | 状态持久化到 storage |
| 适配包 CDN 可用性 | 低 | 内置兜底，拉取失败静默降级，可关闭自动更新 |

---

## 11. 商业模式

**v1.0：完全免费，暂不开源。** Chrome Web Store 免费分发。后续版本根据社区反馈决定是否开源。v2.0+ 可探索增值功能付费。

---

## 12. v1.1+ 场景验证与扩展规划

### 12.1 需求验证框架（"三把刀"）

| 刀 | 问题 |
|----|------|
| 刀1：损失不可逆？ | "看到不该看的内容"后，损失是否无法挽回？ |
| 刀2：平台自己解决了吗？ | 平台是否已有原生方案？ |
| 刀3：时间是正确的切割维度吗？ | 用户想切的是"时间"还是其实是语义？ |

### 12.2 V2 核心场景

| 场景 | 平台 | 结论 |
|------|------|------|
| S1 投资决策噪音 | 雪球 / 股吧 | ★★★★★ v1.0 已做，产品原点 |
| S2a 追剧/追番 | B站评论区、豆瓣、贴吧影视区 | ★★★★★ 窗口模式是近似解，语义级（BYOK）才是完整解；B站动态流因深度硬限制放弃 |
| S2b 体育录像党 | 虎扑、懂球帝、直播吧 | ★★★★★ 开赛时间=绝对切割线，无需语义；平台结构老、投入低 |
| S3 增量续读 | 贴吧直播贴/长楼、知乎长帖 | ★★★★ 窗口模式下半边；存储 `lastRead.<domain>` → URL → timestamp，LRU 50 条 |
| S4 合订本/热点反转考古 | 知乎 + 贴吧 | ★★★☆ 认知污染不可逆；知乎需 `strip_pattern` 剥离"编辑于"并标注 |
| S5 公司近况背调 | 脉脉 / 看准网 | ★★★ 需 `direction: after` 反向过滤；脉脉登录墙限制触达 |

V2 开发顺序建议：S2b 体育录像党 → S3 增量续读 → BYOK 语义过滤 → S4 合订本。

### 12.3 被验证"杀掉"的候选

| 候选 | 死于 | 原因 |
|------|------|------|
| 电商评价（京东/淘宝/点评） | 刀2+刀3 | 平台自带时间排序；真实需求是"找差评"（语义维度） |
| Hacker News | 刀1 | "找回某事件前讨论"需求弱，价值在链接质量而非时效 |
| Stack Overflow / GitHub Issues | 刀2 | 自带时间排序，插件增量小 |
| RSS 阅读器 | 刀2 | 自带未读状态与时间排序 |
| 学术舆论考古 | 技术冲突 | 需快照能力，与"无后端、无快照"冲突 |
| 微博 / B站动态流 | 深度硬限制 | 见 §6.5 |
| 小红书 / 抖音网页版 | 登录墙+加密+虚拟列表 | 投入产出比最差 |

### 12.4 BYOK 大模型扩展（v2.0 差异化）

- **约束**：无后端；用户自配 API Key（DeepSeek / OpenAI 兼容）；显式开启；内容外发需独立同意。
- **语义级剧透防护（核心用例）**：用户配置"《XX》我追到第 3 集"，LLM 判断帖子剧情进度，超前 → 模糊策略。时间切不干净，只有语义能切干净。
- **自然语言过滤**："隐藏转会传闻帖"等，与时间过滤叠加（先时间后语义）。
- **工程**：仅处理可视区（IntersectionObserver）→ 批量 10 帖/次调用 → 本地缓存（帖子 ID → 判定，LRU 1000，7 天过期）→ SPOILER/UNCERTAIN 叠加模糊，SAFE 保持时间结果。
- **成本**：默认 DeepSeek-V3，支持本地 Ollama（零成本）；时间过滤已判定"明显安全"的帖跳过语义调用。
- **隐私**：API Key 仅存 `chrome.storage.local`；Content Script 直连用户 baseURL，不经过自有服务器；独立开关默认关，首次开启明示内容外发。

### 12.5 平台扩展优先级

| 梯队 | 平台 | 关键挑战 |
|------|------|---------|
| 一（低难度×高价值，v1.1~v1.2） | 百度贴吧、知乎、GitHub Issues | 贴吧需分页/正倒序切换；知乎 `strip_pattern` + `history.pushState`；GitHub Issues 自带时间排序、增量小 |
| 二（中难度×高价值，v1.2 剧透线后） | B站评论区、虎扑、懂球帝/直播吧、豆瓣、什么值得买 | 评论区 SPA 待取证；体育社区结构规整 |
| 三（高难度×高价值，v2.0+ 观察） | X/Twitter、脉脉、大众点评 | 虚拟滚动需元素指纹去重；登录墙/反爬 |

> 东方财富股吧、淘股吧、天天基金、集思录属 v1.0 P0 承诺（§4 F3、§9），不在本梯队重复列出；其适配与验收按 §4 F9 闸门在 v1.0 内推进。

基金候选（蚂蚁财富、蛋卷、且慢、好买）仅在满足「稳定网页版 + 独立时间戳 + 历史可回溯 + 类别可识别」后纳入取证，不承诺版本。DOM 取证快照见 `docs/platform-recon.md`。

### 12.6 技术演进

- **SPA 路由监听**：知乎/B站/GitHub 等 URL 变化不触发页面加载，需监听 `history.pushState` / `navigation` 重置内容脚本状态。
- **虚拟滚动去重**：X/Twitter 等滚出视口的帖被回收再渲染，需"已处理元素指纹去重"避免重复计数。

### 12.7 明确放弃清单

| 项目 | 原因 | 决议 |
|------|------|------|
| 微博适配 | 关注流深度硬限制（约 10 页） | 2026-08-12 |
| B站动态流 | 实时信息流，无历史检索 | 2026-08-12 |
| 电商评价 / RSS / Hacker News / Stack Overflow | 见 §12.3 | 2026-08-12 |
| 小红书 / 抖音网页版 | 登录墙+接口加密+虚拟列表 | 2026-08-12 |
| 学术舆论考古 | 需快照，与无后端冲突 | 2026-08-12 |
| 移动端原生 App | 无 DOM 可注入，结构性不可行 | 2026-08-10 |

---

## 附录 A：术语表

| 术语 | 定义 |
|------|------|
| **截止模式** | 只显示发帖时间 ≤ 截止时间 T 的帖子 |
| **窗口模式** | 只显示发帖时间满足 T1 ≤ 时间 ≤ T2 的帖子；判定层已预留，Popup 输入、校验、回显、预设与提示条在 v1.0 完成 |
| **适配包** | 针对单个平台的 JSON 配置（选择器+时间解析规则），非代码，可热更新 |
| **适配包更新通道** | 适配包分发方式——GitHub Releases + jsDelivr CDN，无自建服务端点 |
| **过滤策略** | 被过滤帖子的 DOM 处理方式（隐藏/折叠/模糊） |
| **计数器** | 当前页面被过滤的帖子数量 |
| **时间锚定** | 对相对时间戳，以首次检测到该帖子的时刻为锚点反向推算绝对时间 |
| **按需授权** | optional_host_permissions 模式，安装时不声明全部平台，首次访问时按需请求 |
| **虚拟分页** | 将多个平台原生页中过滤后保留的帖子，按扩展自己的页大小重新组织展示；雪球个股页 v1.0 已实现 |
| **信息流上下文** | 同一站点内由一级/二级类别、筛选或排序形成的独立列表状态；缓存、游标和计数不得跨上下文复用 |
| **补拉引擎** | 面向无限滚动/加载更多页面的通用缺量检测、滚动与排序切换能力（v1.0） |

## 附录 B：竞品对照

| 竞品 | 平台 | 过滤维度 | 与时光机的关系 |
|------|------|---------|---------------|
| Reddit Time Filter | 仅 Reddit | 时间（24h内） | 单平台、停更，时光机可覆盖 |
| NoJunkFeed | 多平台 | 语义/语境 | 不同维度，可共存 |
| Fix My Feed | 多平台 | 关键词 | 不同维度，可共存 |
