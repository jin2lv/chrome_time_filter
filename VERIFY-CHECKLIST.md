# 时光机 — 手动测试 / 真机验收清单

> 版本：2026-08-19｜覆盖：v1.0 全部 P0/P1 功能 + 2026-08-19 深度 Review 修复批次的复核点（标注「Hx/Mx 新行为」）。
> 前置条件：**Windows 本机 Chrome**（`scripts/e2e.mjs` 硬编码 Windows 路径）；已加载 `npm run test:build` 产物（`dist-test/`）或打包 ZIP；雪球/东财建议使用**已登录的日常 Profile**（登录态下东财系无滑块）。
> 风控提示：雪球连续自动翻页/快速点击会触发「访问验证」滑块，污染测试结果。**每次切换类别/页面后等原生列表稳定，降低操作频率**；触发风控即停止测试、休息后再继续。
> 状态标记：`[x]` 通过；`[ ]` 待测；失败时在条目后注明现象（截图/console 报错）。

---

## 0. 测试前基线

- [x] `npm test` 14 个文件全绿（2026-09-09 复跑，含 feed-backfill.test.ts；13 个文件 2026-09-08 复跑；12 个文件全绿 2026-09-06）
- [x] `npx tsc --noEmit` 0 错误（2026-09-09 复跑；2026-09-08；2026-09-06）
- [x] `rm -rf dist && npm run build` 成功（2026-09-09 复跑，check:build-loader 通过：`service-worker-loader.js -> ./assets/service-worker.ts-CrazC4zr.js`；2026-09-06 首次记录 loader 指向 background chunk）
- [x] `npm run test:build` 生成 dist-test/ 并在 chrome://extensions 加载成功、图标正常无报错（2026-09-09 复跑，loader 校验通过；重载后卡片仅剩已知 5 条 optional 权限 redundant 警告（§2.4 定性），无错误；2026-09-08 复跑记录：ID `hmeklmafbbcpmbjikjaffgnnejgeglfe`）

---

## 1. 安装、引导与基础状态

### 1.1 首次安装引导页——✅ 2026-09-09 通过（商店包全新 Profile 实测）
步骤：全新 Profile（或卸载重装）→ 加载扩展。
- [x] 自动打开 welcome.html，3 步引导可走完（2026-09-09：加载未打包商店包后自动打开 welcome.html，3 步「设一个时间点→选过滤策略（隐藏/折叠）→试试在雪球上开启」全部走通，「开始使用」落地 xueqiu.com 新标签）
- [x] 引导完成后插件默认关闭（图标灰色）（2026-09-09：引导完成且未授权时主开关为禁用「—」态、prefs.enabled 默认 false，与「默认关闭」语义一致；授权后进入可用态，见 §2.2）

### 1.2 扩展更新不弹引导页（M2 新行为）
步骤：已有 Profile 上更新扩展版本（改 vite.config.ts version 重新构建加载）。
- [ ] 更新后**不**自动打开 welcome.html（此前版本会弹，M2 修复）

### 1.3 引导写入 prefs
步骤：完成引导 → chrome.storage.local 检查。
- [ ] `prefs.defaultStrategy` 与 `prefs.onboarded` 已写入

### 1.4 扩展重载后的注入稳定性（根治后复验，30e5ecf）——✅ 2026-09-06 真机复验通过，已闭环
背景：根因（CRXJS loader basename 冲突）已于 2026-08-23 修复，`check:build-loader` 已在构建期防回归；本节为真机复验，通过后即可关闭该已知问题。
步骤：重载扩展 → 新开雪球标签（先不打开 Popup）。
- [x] 内容脚本自动注入并生效（无需打开 Popup + 手动刷新）（2026-09-06，沃森生物 SZ300142：重载 test 扩展 → ctrl+T 新开标签导航至个股页，未碰 Popup，约数十秒稳定等待后虚拟分页自动激活：状态条「第 1 页 · 已扫描 2 个原始页」出现、当天(09-06)帖子全部隐藏、截止前帖子保留。备注：刚打开的短暂窗口期内今日帖曾以原生形态可见，属引擎等列表稳定的启动延迟，非注入失败）
- [x] `chrome://extensions` service worker 控制台无未捕获异常，loader 指向 background chunk（2026-09-06；loader 由构建期 check:build-loader 校验通过；重载后 SW 链接转为活动态、注入与虚拟分页全程正常工作；未逐条打开 SW DevTools 控制台核对，以运行证据为准。另：设置页执行 §2.4 移除授权时产生 1 条预期内 Chrome 报错，见 §2.4）

---

## 2. 授权模型（按需授权）

### 2.1 全新 Profile 首次访问（P2 真机 #6）——✅ 2026-09-09 通过（商店包全新 Profile）
步骤：新 Profile 打开雪球 → 点插件图标。
- [x] Popup 显示「此站点未授权」+「授权此站点」按钮，开关禁用，时间控件禁用（2026-09-09：未授权态 Popup 全要素实测——「此站点未授权，点击授权后即可使用」+ 授权按钮 + 主开关「—」禁用 + datetime/6 预设/模式切换全部禁用）
- [x] 授权前预设按钮存在但禁用（同上，6 项预设可见且全部 disabled）

### 2.2 授权流程——✅ 2026-09-09 通过（商店包，个股页过滤实证）
步骤：点击「授权此站点」→ 授权弹窗允许。
- [x] 授权成功后授权区隐藏、时间控件启用、过滤脚本注入当前页（2026-09-09：Chrome 权限弹窗（读取和更改 xueqiu.com 数据）→ 允许 → Popup 授权区消失、全部时间控件/预设启用；注入需刷新页面后生效）
- [x] 页面刷新后过滤生效（新帖隐藏/老帖保留）（2026-09-09：刷新后个股页 SZ300142 虚拟分页激活（「第 1 页 · 已扫描 1 个原始页」），截止=今日0点下窗口内帖（昨天 15:18 等）保留、原生列表隐藏，判定正确；登出态首页信息流触发已知 P3 误弹模态见 §9.1 注记，不影响个股/详情页）

### 2.3 www 子域变体授权（jisilu）
步骤：访问 `www.jisilu.cn` → Popup 授权。
- [x] 一次请求可同时授予 `www.jisilu.cn` 与 `jisilu.cn` 两个 origin（无授权死结）——**2026-09-16/17 真机闭环（商店形态构建）**：设置页点「授权全部金融站点」→ **仅弹出一次** Chrome 权限请求（「时光机」请求获得更多权限 → 读取和更改您在一些网站上的数据），展开「显示详细信息」为一个多站点列表，逐条滚动读到 `finance.eastmoney.com`、`t.10jqka.com.cn`、`www.jisilu.cn`、`xueqiu.com`（及被滚动截断的 `jisilu.cn`）；点「允许」后设置页集思录行 `jisilu.cn / www.jisilu.cn` 显示 **已授权**——**双域在一次 request 内同时授予，无授权死结、无二次弹窗**
- [x] 2026-09-09 开发侧就绪（批次七）：jisilu.json domains 已含双域，设置页单站「授权」按钮一次 request 同时申请两 origin（与 §2.3 同一 permissions.request 机制）

### 2.4 撤销授权立即停用（H2 新行为）——✅ 2026-09-09 通过（商店包真机复验）+ 2026-09-16 新 UI 路径复核
步骤：开启过滤（有隐藏帖子）→ 设置页「已授权站点管理」移除 xueqiu.com → 返回雪球页面（**不刷新**）。
- [x] 已隐藏/折叠的帖子即时恢复显示、占位条消失、悬浮条消失（2026-09-09 商店包（仅 optional 授权，ID phpmhbnkhjefkeedcapibhjidlkkolko）：个股页虚拟分页接管中（原生列表隐藏、虚拟状态条在位）→ 设置页移除 xueqiu.com → permissions.remove 成功 → 切回未刷新的雪球标签：**原生列表与原生分页（1 2 3 4 5 …100 下一页）即时恢复**、虚拟控件消失，PERMISSION_REVOKED → stopFiltering 链路真机闭环）
- [x] 图标立即变灰、角标清空（角标清空实测 ✅；图标颜色在小尺寸截图下未能可靠分辨，如实标注；功能等价证据：Popup 恢复未授权禁用态、过滤链路完全停止）
- [x] 之后滚动加载的新帖**不再**被过滤（观察器已断开）（撤销后页面保持纯原生，滚动/翻页无虚拟接管、无过滤痕迹）
- [x] 刷新页面后确认无过滤效果、无报错（刷新后原生分页/列表原样呈现，扩展完全惰性；SW 控制台仅「Service Worker 已启动」日志，无错误）
- [x] **P2-20 新 UI 路径复核（2026-09-16/17，商店形态构建）**：设置页多站矩阵中雪球行由 `[已授权] [移除]` 点「移除」→ 该行即时变回 `[未授权] [授权]`；随后**未刷新**切回个股页，扩展的虚拟分页接管与过滤全部撤除、页面回到原生形态——per-site 撤销按钮与 §2.4 的 permissions.remove 链路一致（同批「授权全部金融站点」又把雪球一并授回，`[已授权]` 复原）

> 2026-09-09 定性更新：dist-test 的机制限制（manifest 必授权限不可 remove）经商店包（可选授权）真机复验正式解除，本节 4 项全部通过，H2 通路「自动化 + 真机」双覆盖闭环。移除操作在设置页「已授权站点」列表即时生效（xueqiu.com 条目消失，仅剩 localhost）。

### 2.5 设置页授权管理（P2 真机 #7）——✅ 2026-09-09 通过（商店包）+ 2026-09-16 多站矩阵真机复核
- [x] 设置页可列出已授权站点（*:// 条目）（2026-09-09：授权后列表实时显示 xueqiu.com + localhost（manifest 占位），与实际授权状态一致）
- [x] 点击「移除」后站点从列表消失（与 2.4 联动）（2026-09-09：移除 xueqiu.com 后列表仅剩 localhost，与 §2.4 撤销链路同批闭环）
- [x] **P2-20 多站矩阵真机验收（2026-09-16/17，商店形态构建）**：设置页「站点与适配」列出**全部 4 个内置平台**及各自授权状态——雪球 `xueqiu.com`、同花顺 `t.10jqka.com.cn`、集思录 `jisilu.cn / www.jisilu.cn`、东方财富资讯 `finance.eastmoney.com`；未授权行显示 `[授权]`、已授权行显示 `[移除]`，点「授权全部金融站点」后（一次弹窗）四行**全部转为 `[已授权]`**；每行附**数据驱动的适配包版本 + 最后验证日期 + 能力摘要**（雪球 `v0.4.1 · 验证于 2026-09-16 · 帖子过滤/信息流上下文/信息流补拉/个股跨页扫描/详情评论过滤/快捷预设`；其余三站如实标「未真机验证」）；雪球行另有「页面类型能力 (3 类)」展开项与「已记忆时间设置：截止 · 隐藏」+「重置此站设置」；页面底部另有「其他已授权来源（localhost）」与「移除该站点将停止过滤」的说明文案

### 2.6 非目标站点禁用时间控件（M4 新行为）
步骤：打开 Popup 在 github.com 等无适配包页面。
- [ ] 站点显示「未知站点」、时间输入/预设/模式切换全部禁用
- [ ] 无法对非目标域名写入时间设置（chrome.storage.local 无新增 `timeSettings.github.com`）

---

## 3. 时间设置与 Popup 控制面板

### 3.1 截止模式
- [ ] datetime-local 输入可设置分钟级时间，保存后回显一致
- [ ] 默认策略随全局 prefs 同步显示

### 3.2 区间模式与校验（P2 真机 #P2-13）——✅ 2026-09-07 通过（dist-test，7x24/个股页）
- [x] 切「区间」→ 起止输入出现；保存后回显（2026-09-07：区间面板含起止两字段 + 4 预设；保存后悬浮条显示「[区间] 2026-09-06 00:00 至 2026-09-06 23:59」）
- [x] 开始时间晚于结束时间 → 报错「开始时间不能晚于结束时间」，不覆盖上次有效设置（2026-09-07：开始 09-06 00:00 / 结束误输 0005-09-06 时触发该报错，悬浮条仍为原有效区间）
- [x] 只填一端 → 报错「请同时设置开始时间和结束时间」（2026-09-07：切区间未填值即提示该文案；起止字段未填完整时同样提示，语义正确）
- 注：datetime-local 分段手输时「年」段可吸收 6 位数字导致后续错位，为浏览器原生控件行为，非扩展缺陷；预设按钮可规避

### 3.3 快捷预设——✅ 2026-09-07 通过
- [x] 雪球 6 项截止预设按钮齐全：「1小时前 / 今日0点 / 昨日0点 / 今天09:15 / 今天09:30 / 昨日收盘前」（多次会话均见 6 项）
- [x] 每项点击后输入框值正确、保存生效（实测使用过 今日0点/昨日0点/1小时前/昨日收盘前 等多项，悬浮条与过滤即时联动）
- [x] 区间 4 项预设（今天/昨天/近3天/本周）起止值正确（2026-09-07「昨天」预设实测：09-06 00:00 ~ 09-06 23:59）
- [x] 区间金融预设（2026-09-09 批次八新增，dist-test 真机冒烟：区间面板 **7 项预设**渲染齐备——今天/昨天/近3天/本周 + 午间复盘（09:30–11:30）/交易时段（09:30–15:00）/交易日全天；点击「交易日全天」（周三）正确填入当日 00:00–23:59 并保存，悬浮条/过滤联动随保存生效；「最近一个交易日」v1.0 按工作日（不含法定节假日，按钮 title 注明）；解析逻辑单测 13 项（trading-day.test.ts））
- [x] **「午间复盘」真机验收（2026-09-16，交易日正路径）**：交易日（周三）登录态雪球首页 Popup → 区间 → 点击「午间复盘」→ 起止框正确填入 **2026/09/16 09:30 ~ 2026/09/16 11:30**（当地日期时间控件逐字段核对），保存后内容脚本即时重滤；随后以该窗口完成 §5.1 视频类区间复核（窗口外内容全部隐藏）。**交易日正路径（非周末回退分支）至此真机走通**；「最近一个交易日/交易日全天」的法定节假日缺口仍按 v1.0 口径在按钮 title 与文档中标注

### 3.4 边界判定（零误杀/零漏杀实机抽查）
步骤：截止设为 T，找一条时间恰好=T 的帖子与一条 T+1 分钟的帖子。
- [ ] 恰好等于 T 的帖子显示，T 之后 1 分钟隐藏
- [ ] 抽查 10 条，过滤前后与期望一致（对照页面显示时间）

### 3.5 设置持久化（P1 真机 #5）——✅ 2026-09-08 通过（跨会话实证）
- [x] 关闭面板、刷新页面后截止/区间时间保持（2026-09-08：截止 2026-09-06 00:00 于 09-06 写入后，历经扩展多次重载与多次浏览器会话，09-07/09-08 各次打开 Popup 回显一致并持续生效（批次二/三均在会话开头读到该设置）——强于「刷新页面」场景；区间模式保存/回显另见 §3.2/§3.6）

### 3.6 跨 Tab 同步（P1 真机 #9）——✅ 2026-09-07 通过（dist-test）
步骤：A、B 双 Tab 打开雪球 → A 面板改时间。
- [x] B Tab 无需操作自动按新时间重新过滤（storage.onChanged → 广播）（2026-09-07：B 标签 Popup 将截止由 11:20 改为 09-06 00:00 后，切回 A 标签（未做任何操作）悬浮条已变为「截止: 2026-09-06 00:00」且过滤按新边界执行；反向（B 自动应用 A 侧区间）亦验证）

---

## 4. 过滤执行与策略

### 4.1 隐藏策略（P1 真机 #4）
步骤：雪球首页设「1小时前」→ 开启。
- [ ] 新帖隐藏（display:none）、老帖保留，页面无错位

### 4.2 折叠策略（P1 真机 #6）
- [ ] 切换折叠后过滤帖变为「⏳ 此帖被时光机过滤」占位条
- [ ] 点击占位条帖子展开、占位条移除；再次设置后重新折叠
- [ ] 展开恢复的帖子布局与站点原生一致（M5：原 inline display 被还原，无布局跳动）

### 4.3 关闭/开启恢复（P1 真机 #7）——◐ 2026-09-08 部分通过（隐藏策略链路）；2026-09-09 商店包+虚拟分页形态补强
- [ ] 关闭过滤 → 全部恢复显示（含折叠展开态）；重新开启 → 再次过滤（2026-09-08 详情页实测：关闭→被过滤评论全部恢复；开启→重新过滤（§7.4 记录）；折叠展开态的恢复仍待真机）
- [ ] 计数归零/恢复正确（2026-09-08：关闭后 Popup「已过滤 0 条」，重新开启后重新累计；快捷键关闭链路 §6.7b 已由用户实机验证）
- [x] 2026-09-09 补强（商店包全新 Profile，个股页虚拟分页形态）：Popup 关闭过滤 → 虚拟分页控件移除、原生列表/原生分页即时恢复；重新开启 → 虚拟分页重建、扫描状态行恢复——关闭/开启双向链路在虚拟分页形态下闭环（§13 流程内实测）

### 4.4 无法解析回退
- [ ] 无法解析的帖子默认显示，Popup 与悬浮条标注「N 条无法解析」

### 4.5 计数与角标（P1 真机 #8）——◐ 多批次实机观察通过，两项保留
- [ ] Popup「已过滤 N 条」与图标角标随滚动实时变化（200ms 内可达最终值）（多批次实机观察：角标 25/15/18/10 与 Popup 计数随过滤状态实时联动；200ms 阈值未正式计时；遗留 P3 观察项——懒加载评论分批被过滤时角标未即时增长而 Popup 计数正确，见 DEV-PLAN 09-08 批次记录）
- [ ] 切换时间/关闭过滤后计数重置（2026-09-08：关闭过滤后计数清零、重新开启后重新累计（§7.4）；类别切换计数隔离 §5.1 已验证）

### 4.6 悬浮提示条（P2 真机 #9）——✅ 2026-09-06/07 通过
- [x] 设置页开启 → 页面右下角出现，内容含边界时间与已过滤数（2026-09-06：实测内容「⏳ 时光机已激活 | 截止=… | 已过滤 N 条 | 仅过滤已加载内容」，区间模式显示「[区间] 起 至 止」；个股页还含「已扫描 N 个原始页」）
- [x] 滚动加载时内容不闪烁/不抖动（M3 节流后每 250ms 至多更新一次）（观察滚动/翻页期间内容平滑递增，无重建闪烁）
- [x] × 关闭后消失；设置页关闭后不再注入（2026-09-07：测毕经设置页关闭开关，恢复原始关闭状态）

---

## 5. 雪球首页类别信息流（P2-14 / P2-16）——✅ 2026-09-07 十类全量通过（dist-test，截止多档实测，零风控）

### 5.1 一级类别切换——✅ 通过
步骤：登录态首页，逐一切换 关注/热门/7x24/自选/视频/基金/资讯/达人/私募/ETF。
- [x] 每类切换后过滤按当前时间设置生效（2026-09-07：截止 09-05 18:00 / 09-07 06:14 两档下逐类验证；各类可见帖均早于截止，越界帖隐藏；含「N小时前」相对时间（锚定解析）与「修改于…」编辑格式样本）
- [x] 切换时旧类别扫描/观察器取消，无旧上下文串流（每类计数独立重置：关注 15 → 热门 30 → 7x24 10/30/42 等，未出现跨类累计）
- [x] 每类计数隔离（切类后从该类别自身计数开始）（Popup 与悬浮条计数均随类别重置）
- [x] 区间模式补充抽查（2026-09-08：区间=今天 00:00~23:59，切换间隔 ≥5s：关注流仅保留 2 条今日帖（「1小时前」张琛/钢哥，越界帖隐藏，已过滤 18）；热门流 10 条全部为往日帖被隐藏（可见 0 条、角标 10）；两类别计数独立、无串流。首页区间版 §5.1 至此覆盖截止+区间两模式，其余类别区间版留待后续按需补测）
- [x] 区间版其余类别抽查（2026-09-09：区间=今天 00:00~23:59，逐类切换间隔 ≥6s：**自选**✅ 可见帖均今日（45分钟前/1小时前）、过滤 7；**基金**✅ 加载内容全为窗口外、10 条全隐、可见 0；**资讯**✅ 可见 1 条今日帖（闪迪 1小时前）、过滤 7；**达人**✅ 8 条全隐；**私募**✅ 10 条全隐；**ETF**✅ 8 条全隐。可见帖均在窗口内、计数逐类独立重置（7/10/7/8/10/8）、无串流。**视频 ⚠️ 存在适配覆盖缺口**：窗口外视频卡片不被过滤（但斌 08-19 13:08、陈立峰 昨天 06:59、特斯拉 08-24 均可见），混排普通帖正常过滤、Popup 类别标注正常——定性为视频卡片 DOM 不匹配 post_selectors（`article.timeline__item` 等），完全绕过过滤链（无隐藏、无 unparseable 计数）；09-07 截止模式该类通过系样本未含窗口外视频卡片。待适配包 v0.4.1 增补视频卡片选择器后复验，详见 DEV-PLAN 09-09 批次记录新观察 O1）
- [x] **视频类复核（2026-09-14，O1 降级为不可复现）**：登录态 `视频` 类别、截止=2026-09-06 00:00 下浮层探针取证——`videos: 8` 且 `matched: 8`（**视频卡片全部命中 `article.timeline__item`**，视频位于卡片内 `div.video_box.timeline-video`），窗口外的「昨天 23:12 / 23:11 / 21:32」三张视频卡片 `display:none` 已隐藏、窗口内卡片正常显示——**视频帖确实在过滤链内**，09-09 记录的「完全绕过过滤链」在当前 DOM 不成立；**未修改适配包**（今日实测 `.video__box` 类不存在，若按当时结论猜测增补选择器即为无证据改动）
- [x] **视频类区间模式复核（2026-09-16，O1 正式关闭）**：登录态 `视频` 类别 + 区间=今天 09:30–11:30（Popup「午间复盘」预设）下探针取证——`art.tli=8 / article=8`（8 条视频卡片全部命中 `article.timeline__item`）、**`filtN=8` 即 8 条全部被过滤隐藏**（`data-tm-filtered="1"`），`bar=null`（视频类不在补拉白名单，无操作条，符合设计）。**区间模式同样不存在「视频卡片绕过过滤链」**：09-09 的 O1 观察在截止与区间两种模式下均不可复现，定性为当次样本/观测误差（09-07 截止样本未含窗口外卡片、09-09 区间样本误判）。O1 结案，不产生适配包改动

### 5.2 二级类别——✅ 通过（关注流内容为空，标签渲染验证）
- [x] 关注流：「全部/关注精选/只看原发/未分组」各自独立上下文（2026-09-07：四标签渲染正常；该账号关注流无内容，深度验证沿用 2026-08-14/17 登录态校准记录）
- [x] 自选流：「全部/公告/新闻」各自独立上下文（2026-09-07：公告二级实测——条目均为「昨天 15:34–19:07 来自公告」，独立计数为 0，全部正确保留）
- [ ] 动态自定义分组出现时补测（P2-14 遗留）

### 5.3 7x24 时间解析——✅ 通过
- [x] 「今天」/「昨天」分组标题 + 行内 HH:mm 组合正确；今日快讯隐藏、昨日保留（对照截止时间）（2026-09-07：截止=09-07 06:14 时「今天」分组标题保留、06:14 之后条目全部隐藏，06:02/05:37/04:08/03:39 保留，昨日 09:39 同题快讯保留；截止=今日0点时今日条目全隐、分组标题保留——分组+行内组合解析与分钟级边界正确）

### 5.4 智能流完整性标注（P2-15）——✅ 通过
- [x] 首页普通类别 Popup 显示「当前类别为智能或热度信息流，仅过滤已加载内容」（2026-09-07：关注/7x24/ETF 等各类 Popup 均带类别名前缀，如「7x24：当前类别为智能或热度信息流，仅过滤已加载内容，结果不代表完整时间范围。」）
- [x] 悬浮条含「仅过滤已加载内容」（2026-09-06/07 悬浮条尾部恒有该标注）

### 5.5 连续滚动 3 屏（P2-16）——✅ 通过
- [x] 滚动追加 3 屏无重复帖子被重复计数、无计数错乱、无旧上下文内容混入（2026-09-07 ETF 流 3 屏：三屏作者/内容零重复，计数稳定，无越界帖混入）

### 5.6 信息流连续补拉（P2-17 切片 1 真机验收）——✅ 2026-09-09 通过（dist-test，登录态雪球首页，零风控）
> 引擎：`src/content/feed-backfill.ts`（适配包 v0.4.0 backfill：contexts=["7x24","关注 / 全部"]、1200ms/屏、上限 10 屏、目标 10 条、连续 2 屏无新增判末页）。

- [x] 白名单挂载：7x24 与「关注 / 全部」出现底部操作条「查找更早的帖子」；非白名单类（自选/视频/基金/资讯/达人/私募/ETF）无操作条（2026-09-09 实测两类挂载 + 七类不挂载）
- [x] 手动触发自动滚动与命中即停（截止=1小时前，2026-09-09 ~05:48）：7x24 点击后自动滚动，**1 屏命中 10 条即停**，状态条「已找到 10 条更早的符合时间条件帖子（已加载 1 屏）」+ 按钮变「继续查找」；新拉出的更早帖保留可见、今日帖（截止后）保持隐藏、悬浮条计数同步（已过滤 4 条不变）；关注流复测 **1 屏命中 15 条**（>目标即停）、计数独立
- [x] 末页判定（exhausted）：截止=今日0点（凌晨时段今日 7x24 仅 ~30 条）：滚动追加 3 屏后站点切换为「加载更多」按钮模式（站点行为，load-more 按钮式获取属切片 2），连续 2 屏无新增 → 状态条「已到信息流末页，共找到 0 条符合时间条件（已加载 3 屏）」+ **按钮按设计禁用**；Popup 同步显示「已加载 3 屏，到达信息流末页。」
- [x] 取消（cancelled）：运行中点击「取消」→「补拉已取消，共找到 34 条符合时间条件（已加载 6 屏）」+ 按钮恢复「继续查找」（可用）；启动后立即取消的窗口极短（1200ms/屏），实测用 double-click（两次 click 间隔 ~100ms）稳定复现——第一击启动、第二击命中 loading 态取消
- [x] 区间模式补拉：区间=今天 00:00~23:59 下 7x24 补拉 1-2 屏命中 10/20 条即停（窗口内今日帖），新拉帖保留、窗口外隐藏，与截止模式行为一致
- [x] 屏数状态双语义：悬浮条实时显示「已扫描 N 屏」（screens 单位渲染正确；注：文案为「已扫描」非任务描述的「已加载」，语义差异如实记录）；Popup screens 语义文案 exhausted/cancelled 两态真机捕获；**Popup loading 态文案（「正在查找更早的帖子，已加载 N 屏。」）未在真机捕获**——单次运行窗口 1.2~2.5s 短于打开 Popup 的操作延迟（调试标签页方案亦未命中实时刷新），如实标注；loading 渲染管线由同函数其余三态真机 + feed-backfill.test.ts 单测覆盖，取消成功本身证明 loading 态与按钮切换正确
- [x] 命中统计不双计：补拉新增帖由既有 observer → processPost 链唯一负责过滤/隐藏/计数（今日帖计数 10→30 随滚动递增），decide 仅作命中统计
- 附（站点行为记录）：~~雪球 7x24/关注流滚动追加约 2-3 屏后出现「加载更多」按钮，纯滚动不再追加~~ **该记录于 2026-09-16 被真机取证推翻**：7x24 与「关注 / 全部」两上下文全程未出现可见的「加载更多」按钮，见下条。

#### 5.6.1 P2-17 切片 2 真机回归 + 雪球「加载更多」选择器取证（2026-09-16，dist-test，登录态，零风控）

> 取证方法：雪球页面挂 DevTools 会触发其反调试 `debugger` 陷阱，故改用**临时内容脚本探针**（隔离世界读 DOM → 固定浮层渲染 → 截图/无障碍树读取）。另发现**该页类目标签区域合成点击不可靠**，探针内置「地址栏 hash 通道」（`#tm-tab-<类目名>` 由探针代点标签）完成切类目；探针取证后整体移除（`src/content/dom-probe.ts` 删除、`index.ts` 还原，`git status` 干净）。

- [x] **7x24 结构取证**：`div.timeline__live` = 分组头 `div.home__timeline-live__hd`（今天/昨天）+ `table.home__timeline-live__tb`；**`.timeline__live tr` 命中有效**（实测 10 行，滚动后 30 行），全部被过滤（`filtN=10→30`、`data-tm-filtered="1"`、`data-tm-orig-display` 记录原样式）
- [x] **「加载更多」按钮取证与决策**：候选元素为 **`a.home__timeline__more`**（文本「加载更多」），末页标记为 **`span.home__timeline__no-more`**（「没有更多内容了」）；**两上下文补拉全程二者均 `display:none`、尺寸 0×0**——雪球首页由滚动触发无限加载，**不存在可见的按钮式获取**。故适配包按防御性声明 `load_more_selector: a.home__timeline__more`（引擎 `canLoadMore()` 有可见性 + disabled 门控，不可见时不会点击），并**修正 `feed-backfill.ts` 中「雪球实测滚动 2-3 屏后切换为按钮式」的无据注释**；适配包升 v0.4.1（`last_verified=2026-09-16`），`adapters.json`/`.sha256` 同步重生成
- [x] **滚动补拉有效性（7x24，截止=昨日0点）**：点击「查找更早的帖子」→ `window.scrollTo` 生效（`y 0→1129`、`docH 2060→2040`，行数 **10→30**），站点懒加载正常触发；逐屏累加计数正确（终态「已加载 3 屏」）
- [x] **命中路径与逐屏累加（关注 / 全部，截止=今天09:15）**：点击后 **1 屏新增 15 行、命中 16 条**（`已找到 16 条更早的符合时间条件帖子（已加载 1 屏）`，按钮变「继续查找」），行数 **15→30**、`docH 2717→7884`、`y=1806`；新拉出的更早帖（昨天 23:42/23:36 等）保留可见、截止后帖子保持隐藏，命中统计不双计（过滤/计数仍由 observer 链唯一负责）
- [x] **末页判定（exhausted）与文案**：连续 2 屏无新增 → 页面浮动条「**已到信息流末页，共找到 0 条符合时间条件（已加载 2/3 屏）**」+ 按钮按设计禁用；Popup 同步显示统一文案「**已加载 3 屏，到达信息流末页。**」（`shared/scan-text.ts` 双端一致，真机捕获）
- [x] **白名单与不挂载**：7x24、关注/全部 挂载操作条；自选/视频/基金/资讯/达人/私募/ETF 无操作条（`bar=null`，2026-09-16 于视频类目复核时确认）
- [x] **Popup 计数联动**：`已过滤: 30 条`（7x24 全隐态）与探针 `filtN=30` 一致；切类目后 Popup 计数按新上下文重置（关注/全部 = 11 条）
- [ ] **取消（cancelled）本次未重测**：单次运行窗口仅 1.2~2.5s（关注流 1 屏即达目标 10 条而回到 idle），短于工具点击延迟，未能在真机稳定命中 loading 态；该状态已由 2026-09-09 真机 double-click 复现记录 + `feed-backfill.test.ts` 单测覆盖，本次不重复
- [ ] **屏数上限（limit，10 屏）本次未走到**：雪球 7x24/关注流在 2-3 屏内即停滞（站点不再懒加载），无法自然累积到 10 屏；`limit` 分支保留单测覆盖，真机待长流平台样本
- **新发现（缺陷，本批已修，真机复验待下批）**：**窗口已处于文档底部时重复触发补拉会误判「已到信息流末页」**——`window.scrollTo(bottom)` 在已到底时为空操作，站点不再懒加载新行，连续 `end_stall_count=2` 屏后进入 exhausted（实测 2 屏即判末页，而信息流实际仍有内容）；且 exhausted 后按钮禁用，用户需切换类目/改设置才能重挂载重试。**修法**：新增 `scrollToFeedEnd()`——文档确实可滚动且 `scrollY` 已在底部（容差 2px）时，先回退一屏、`await sleep(min(250ms, scroll_delay_ms))`（两次同步滚动会被合并成一帧，必须留可观察间隔），再滚到文档末端；未到底时行为不变。单测覆盖新增 2 项（已到底先回退再滚到底 / 未到底不产生多余回退）；**该修法的真机复验列入下一设备会话**（预期：已在底部再次触发不再 2 屏即报末页，而是重新拉到内容或继续累计屏数）

---

## 6. 雪球个股页虚拟分页（P2-12 / P2-16 / P2 真机 #13-14）

> 风控重点：每步之间等待原生列表稳定，翻页频率放缓。

### 6.1 六类别全量终验（P2-16，08-17 记录「交易」类别受风控污染未终验）——✅ 2026-09-06 通过（样本：沃森生物 SZ300142，截止=今日0点→09-05 18:00，隐藏策略）
- [x] 全部 / 讨论 / 投资者关系 / 交易 / 资讯 / 公告 六类各自：虚拟第 1 页 10 条、「第 1 页 · 已扫描 N 个原始页」、原生 `.status-list`/`.pagination` 隐藏（2026-09-06 逐类实测：全部/讨论=已扫描 2 页，投资者关系=2 页（2014 年调研问答，全部保留），**交易=1 页、10 条模拟盘交易帖、未触发风控滑块（553fd11 节流生效，08-17 遗留项闭环）**，资讯=1 页，公告=1 页；各类内容互不相同且时间序合理，全程零「访问验证」）

### 6.2 首屏聚合（P2 真机 #13）——✅ 2026-09-07 通过（沃森生物 SZ300142）
- [x] 从原始第 1 页开始按需扫描，前 10 条符合时间的帖子组成虚拟第 1 页即停止（截止=今日0点/昨日0点两档实测：第 1 页恰 10 条即停，已扫描页数=恰好满足所需）
- [x] 首屏全空场景（截止激进）不出现空白：跨页聚合出内容并显示扫描页数（截止=09-06 00:00 时原生第 1 页多数为越界帖，引擎跨 2 个原生页聚合出 10 条 09-05 帖，无空白屏）

### 6.5 终态提示——⚠️ 2026-09-07 部分通过（深度不足）
- [x] 到达末页显示「已到末页」；达到 200 页显示「已达上限」；原生页加载失败显示「原始页面加载失败」（**「原始页面加载失败」已真机实测**：2026-09-07 区间深回溯扫描中触发雪球风控，原生页加载失败后状态条正确显示该文案并停止；「已到末页/已达上限」因交易类回溯至 2025-08 深度过大未走到，由自动化覆盖）

### 6.6 扫描中取消（P2 真机 #14）——✅ 2026-09-07 通过
- [x] 扫描中「取消扫描」→ 状态变「扫描已取消」，页面保留已收集的虚拟页（2026-09-07：区间 09-06 深回溯扫描至第 59 原生页时点击「取消扫描」——扫描中状态条显示「正在扫描原始第 N 页 + 取消扫描」，点击后 Popup 显示「扫描已取消，已扫描 59 个原始页面。」，已收集虚拟页保留且第 1 页内容正确）

### 6.7 扫描中修改时间/关闭（P2 真机 #14）——✅ 2026-09-07 部分通过
- [x] 扫描中修改截止时间 → 旧扫描停止，按新边界重新聚合（2026-09-07：两次扫描进行中修改截止（09-06→09-05→09-04），旧扫描立即停止、已扫描数按新边界重计、列表按新边界重聚合）
- [x] 扫描中关闭过滤 → 虚拟控件移除，原生列表/分页恢复显示（2026-09-08：Alt+Shift+T 绑定后用户实机验证通过；TOGGLE_FILTER → reapplyAll 恢复 DOM 通路亦有自动化覆盖）

### 6.9 类别切换重建（P2-12）——✅ 2026-09-07 通过
- [x] 六类之间来回切换：旧会话取消、新类别从第 1 页重新聚合、缓存隔离（2026-09-07：全部→交易→（次日）全部→交易多次往返，每次均从第 1 页重新聚合、计数/缓存隔离正确）

### 6.3 下一页续扫与去重（08-17 记录待最新构建复验）——✅ 2026-09-06 通过（讨论类）
- [x] 点击「下一页」才继续扫描（不预取超过所需页）（已扫描 2→3，仅多扫 1 个原生页即收满第 2 页 10 条）
- [x] 第二页与第一页无重复 ID（data-id 去重生效）（第 2 页 10 条与第 1 页零重叠；同作者不同帖正常共现）

### 6.4 上一页缓存（08-17 记录待复验）——✅ 2026-09-06 通过（讨论类）
- [x] 「上一页」直接出缓存，不增加已扫描原生页数、不重新请求（回翻后状态保持「第 1 页 · 已扫描 3 个原始页」，内容与缓存一致）

### 6.8 空类别（08-17 已通过，供回归）
- [ ] 「投资者关系」无内容时保留原生「该股票暂无信息」，无虚拟控件、无报错
- [ ] 从空类别返回「讨论」→ 虚拟分页恢复正常

### 6.10 原生隐藏抵抗
- [ ] 扫描期间原生列表/分页被站点异步改回 display:block 时，1 秒内重新隐藏（属性观察器）

---

## 7. 详情页评论过滤（P2-4 / P2-16 / P2 真机 #3-4）

### 7.1 评论独立判定——✅ 2026-09-06/07 通过
- [x] 评论按自身时间戳与截止/区间独立判定（老帖新评论被隐藏，保持时间隔离感）（2026-09-06：详情页唯一今日评论被独立隐藏而正文保留；2026-09-07：详情页 2 条 09-03 评论在截止=09-04 下正确保留、计数独立于列表）

### 7.2 「今天/昨天 HH:mm」评论（P2 真机 #3 + H1 复核）——✅ 2026-09-06 通过（分钟级精确判定实机双向验证）
步骤：找一条「昨天 HH:mm · 省份」评论，截止设为该帖时刻之后（应保留）/ 之前（应隐藏）。
- [x] 「昨天 HH:mm」按具体时刻判定（H1 修复后不得出现小时级漂移）（2026-09-06，云海总帖详情页评论「09-05 17:59 · 云南」：截止=09-05 17:00 → 评论隐藏（已过滤 1 条、角标红色 1）；截止=09-05 18:00 → 评论恢复显示。分钟级精确，双向符合。注：雪球详情页评论列表现显示绝对「MM-DD HH:mm · 省份」格式，「昨天 HH:mm」字面格式见于信息流帖子 meta 与正文引用块（如「昨天 20:12」），在截止=今日0点下正确保留；未在详情页评论区找到「昨天 HH:mm」字面样本，或为雪球展示格式变化，判定通路（相对/绝对→同一比较函数）一致）
- [x] 「今天 HH:mm」按当日时刻判定（2026-09-06，另一详情页唯一评论为今日发布 → 被独立隐藏（评论区空白而正文保留），符合评论独立判定 + 截止=今日0点）

### 7.3 评论无时间戳回退（P2 真机 #4）
- [ ] 回退策略=全部显示：无时间戳评论显示、计数计入「无法解析」
- [ ] 回退策略=默认折叠：无时间戳评论折叠为占位条
- [ ] 切换策略后行为立即更新

### 7.4 区间模式评论双向样本（08-17 已通过，供回归）——✅ 2026-09-08 通过（单页双向，dist-test）
- [x] 区间内评论保留、区间外（含「今天」）隐藏（2026-09-08：区间=今天 00:00~23:59，样本页 xueqiu.com/6451611049/141390314（2020-02-17 老帖，因被今日转发热帖引用而当天仍在新增评论，评论时间跨 2020-02-17 至当日，天然双向样本）：区间内评论（「14分钟前」「56分钟前」「今天 07:23」「今天 07:12」共 4 条）全部保留；区间外评论（2020-02-17 批次等，Popup 计数 80 条）全部隐藏。双向对照法：Popup 临时关闭过滤 → 区间外评论整体显形（可读时间戳 2020-02-17 11:24/11:39/11:57/17:33 等）→ 重新开启 → 区间外评论重新隐藏、今日评论保留。附加：切回截止 2026-09-06 00:00 后同页评论即时重滤（今日评论转为隐藏、最新可见评论为 2025-12-08），评论独立判定+广播通路再次确认。注：详情页正文（区间外老帖）按设计不过滤（§9.3 评论独立过滤语义），仅评论区参与判定）

---

## 8. 快捷键（P2-7）——✅ 2026-09-08 通过（suggested_key 更换 + 手动绑定 + 实机按键验证）
- [x] `Alt+Shift+T`（原 Ctrl+Shift+T，与 Chrome 内置「重新打开已关闭标签页」冲突被抢占，2026-09-07 实测）切换当前标签页过滤开关，图标彩色/灰色同步（2026-09-08：suggested_key 更换后在 `chrome://extensions/shortcuts` 手动绑定 Alt+Shift+T，用户实机按键验证开关与图标正常）
- [x] 未设定时间/未授权页面按快捷键无效果、图标状态不变（M6 守卫）（2026-09-08 用户实机确认无异常）
- 附（重要运维知识）：manifest suggested_key 变更后**重载/更新扩展不会自动应用绑定**（实测 shortcuts 页仍显示「未设置」），首次必须手动绑定一次；且快捷键捕获框不接收合成/自动化按键，绑定只能人工

---

## 9. 失效检测、诊断与隐私

### 9.1 失效模态（P2 真机 #8 / P1-5）——✅ 2026-09-08 通过（构造法，dist-test）
步骤：devtools 篡改适配包 `post_selectors` 使其零匹配（或临时改配置）→ 首页刷新。
- [x] 5s 内出现不可关闭「页面结构可能已变化」模态，按钮「刷新重试」（2026-09-08：直改 dist-test/assets/adapters-*.js 将雪球三个 post_selectors 替换为零匹配 `.tm-no-such-2026`（备份后篡改，包结构仍过 validateAdapter）→ 重载扩展 → 刷新 xueqiu.com 首页 → 模态出现：遮罩+卡片「⚠️ 时光机需要更新」+「雪球 页面结构可能已变化，过滤已暂停。请点击下方按钮刷新页面重试。」+「刷新重试」按钮；a11y 树核实模态内无任何关闭/取消控件（不可关闭），点击「刷新重试」为唯一出口（location.reload）。构造期同页帖子不再隐藏、角标清空，符合「过滤已暂停」语义。测毕已还原干净构建并重载确认过滤恢复（角标 15））

### 9.2 个人主页/搜索页不误报（H4 新行为）——✅ 2026-09-06 通过
步骤：访问 xueqiu.com 某用户主页、搜索结果页，等待 6s。
- [x] 无失效模态、无任何注入痕迹，页面不受影响（2026-09-06：用户主页 xueqiu.com/u/1919460849 与搜索页 xueqiu.com/k?q=沃森生物 各等待 ≥6s，均无模态、渲染正常）

### 9.3 详情评论页不误报（H4 新行为）——✅ 2026-09-06 通过
步骤：打开帖子详情页（正文 + 评论结构），等待 6s。
- [x] 无失效模态（此前版本可能误报）（2026-09-06：两个帖子详情页（xueqiu.com/5651253930/408197301、xueqiu.com/1184301458/408182495）各等待 ≥6s 均无模态）

### 9.4 诊断明细——✅ 2026-09-08 通过（构造法，dist-test）
- [x] Popup 展开「查看无法解析的时间」能看到 {kind, page, context, raw} 样本（≤8 条）（2026-09-08：基于干净备份仅篡改雪球 timestamp.selector 为不存在元素（post_selectors 保持原样，无误报模态）→ 重载 → 刷新首页 → 全部帖子 extractTimestampText 返回 null（16 条无法解析、默认显示、已过滤 0）→ Popup 出现「查看无法解析的时间」展开区 → 展开后样本 1 条：「帖子 / (关注 / 全部)：未找到时间元素」（addDiagnostic 按 kind+raw 去重，远低于 8 条上限；字段齐全，context 为当前首页类别「关注 / 全部」））
- [x] 无法解析回退为默认显示、计数标注（同上：16 条无法解析，帖子全部保持原生可见，Popup 显示「已过滤 0 条」）

### 9.5 诊断报告脱敏（P2-19 部分）——✅ 2026-09-08 通过（构造法，dist-test）
- [x] 「复制诊断报告」仅含平台/适配包版本/模式边界/计数与 pathname+时间原文（2026-09-08：点击「复制诊断报告」，powershell Get-Clipboard 核验全文 4 行——`时光机诊断报告 | 平台=雪球 | 适配包内置版本=0.3.0` / `模式=cutoff | 边界=截止 2026/9/6 00:00:00` / `已过滤 0 条 | 16 条无法解析` / `- 帖子 | 页面=/ | 上下文=关注 / 全部 | 原始时间=未找到时间元素 | 回退行为=默认显示`）
- [x] 报告不含 URL query、帖子正文、账号、Cookie（同上逐项核对：页面字段仅 pathname「/」无 query；无任何帖子内容/用户标识/凭据字段）

---

## 10. 其他平台适配（P2 真机 #1-2 / 2026-08-18 欠账）

### 10.1 同花顺 t.10jqka.com.cn
- [ ] 首页信息流授权后按 MMDD（data-date）+ HH:mm 组合时间过滤
- [ ] 跨年帖正确回退一年；缺年份补当年
- [ ] 个股吧/圈子页面无适配（产品线废弃），静默无异常

### 10.2 集思录 jisilu.cn（08-18 内置包，真机验收欠）
- [ ] 分类列表（/category/4 等）按「作者 回复 • YYYY-MM-DD HH:mm」解析过滤
- [ ] 详情回复（/question/{id}）按各自时间过滤
- [ ] 大帖回复分页/懒加载机制观察记录（recon 遗留）
- [ ] 首页 explore（无时间戳路径）静默退出、无失效模态

### 10.3 东方财富资讯 finance.eastmoney.com（08-18 内置包，真机验收欠）
- [ ] `/a/czqyw.html` 与 `/a/cgspl.html` 列表按 `YYYY年MM月DD日 HH:mm` 过滤
- [ ] `_{n}.html` 分页逐页可过滤
- [ ] 正文页（/a/数字.html）静默退出（active_paths 白名单外）

---

## 11. 适配包热更新（P2-5 / P2-19 / P2 真机 #10）

> 真实发布源已接通（2026-09-09 批次九）：`cdn.jsdelivr.net/gh/jin2lv/chrome_time_filter@main/adapters.json` + `.sha256`；机制层面（schema/校验和/版本单调/应急停用/静默降级）已有 25 项自动化断言覆盖。

- [x] 安装时触发一次拉取；网络失败/404 时静默降级内置包（SW 控制台看日志，无用户可见报错）（自动化已覆盖 fetch 失败/404/校验和缺失→静默降级）——**真机闭环 2026-09-16/17（商店形态构建）**：Service Worker 控制台读取 `chrome.storage.local['adapters.remote']` = **`{"package":{…4 平台…},"updatedAt":1789570549793,"version":"1.0.0"}`**，即安装时拉取 → sha256 校验 → schema 校验 → 写入存储全链路在真机成立；同批 `curl -I` 复核 CDN 可达（HTTP/2 200），排除「网络不可达导致静默降级」的解释
- [ ] 设置页开关关闭后不再请求（自动化已覆盖 `disabled` 短路）
- [ ] （mock 环境已覆盖：远程版本更高 → 存储更新 + 广播 + 内容脚本重扫；真机可配合本地 HTTP 服务验证一次完整链路）
- [ ] 完整性校验：`.sha256` 不匹配时丢弃且不覆盖已存远程包（自动化已覆盖 2026-09-09）
- [ ] 应急停用：远程包 `disabled: true` → 清空远程包回退内置 + 广播（自动化已覆盖 2026-09-09）
- **新发现（产品问题，2026-09-16 真机取证）**：**远程包会「遮挡」更新的内置包**——真机存储里的远程包是**旧内容**（内含雪球适配包 `last_verified: 2026-09-09`、`backfill` 不含 `load_more_selector`），而同一构建的内置包已升到 v0.4.1（`last_verified: 2026-09-16`、含该选择器）。因为版本单调比较的是「远程 vs 已存远程」而非「远程 vs 内置」，**只要 CDN 上存在任何远程包，它就会持续覆盖较新的内置包**（除非 CDN 版本号递增或置 `disabled`）。⇒ 运维纪律：**每次改动内置适配包后必须同步 `npm run adapters:build` → 提交 → 推送（必要时递增 adapters.json 版本号 + purge CDN）**，否则用户端跑的是旧适配内容
- [x] **发布源同步完成（2026-09-17）**：按上述纪律将 `adapters.json` 版本递增至 **1.0.1**（内容含雪球 v0.4.1 + `load_more_selector`）→ 提交 `3d1f2d5` → 推送 `origin/main`（`4415950..3d1f2d5`）→ 对 `adapters.json` 与 `adapters.json.sha256` 各 purge 一次 → **CDN 复核三方一致**：CDN 正文 SHA-256 = CDN `.sha256` 文件 = 本地 `adapters.json.sha256` = `0d58e430…`，CDN 版本 1.0.1、`last_verified: 2026-09-16`、`load_more_selector: a.home__timeline__more` 均就位（首次 purge 后约 40s 内 CDN 仍返回旧内容，第二次 purge 后 60s 复核已更新——**purge 有传播延迟，需复核而非假定立即生效**）
- [ ] **远程版本递增的设备侧落地**（fetch → 校验 → 版本单调 → 存储更新 → 广播重扫）：**本次未观察到**，如实记录。原因：`updateAdapters()` 只在 `onInstalled(reason='install')` 与 12h alarm 触发；本轮已推送但设备端存储仍是 1.0.0（其安装时 alarm 于次日约 12h 后才会再触发）。非破坏性强制触发在本机不可行——SW 控制台里 `import()` 被 Service Worker 规范禁止（`import() is disallowed on ServiceWorkerGlobalScope`），SW 也**没有**「手动检查更新」的消息入口（`onMessage` 仅处理 `FILTER_COUNT_UPDATED`/`FILTER_STATE_CHANGED`）；重装虽会立即触发但要重走授权。⇒ 观察方式：等下一个 alarm 周期后在 SW 控制台读 `chrome.storage.local['adapters.remote'].version` 应为 1.0.1（**已登记为待观察项**）；亦记录一条产品观察：商店更新（reason='update'）同样不会立即拉取，用户最长需等 12h 才拿到新适配包
- [x] 发布源与产物端到端核验（2026-09-09 批次九，真实 CDN）：`cdn.jsdelivr.net/gh/jin2lv/chrome_time_filter@main/adapters.json` 与 `.sha256` 均 HTTP 200；远端正文 SHA-256 = 远端校验和文件 = 本地 `adapters.json.sha256`，且**逐字节等同本地文件**（v1.0.0，4 平台）——客户端完整性校验在真实基础设施上成立

---

## 12. 性能（P1 真机 #10，P1-4）——✅ 2026-09-08 通过（playwright Chromium + 结构化 mock 雪球页实测）

> 方法与边界：playwright Chromium（本机 Windows 有头）+ 加载 dist-test + 本地 mock 雪球 DOM（结构与适配器选择器一致：首页 80 帖/屏、个股页虚拟分页 SPA 换页）。未在登录态真站测量（computer-use 会话不可用；真站登录态复核可选后补，工作量指标对 DOM 结构敏感、对站点品牌无关）。指标采集：PerformanceObserver('longtask') + requestAnimationFrame 帧采样 + setTimeout 节拍事件循环滞后探针。

- [x] 无明显卡顿（2026-09-08：80 帖首屏 + 3 屏滚动追加（每屏 20 帖，含 ~50% 待过滤帖）全程 **0 个 >50ms 长任务、平均 60fps、0 个 >100ms 慢帧**；首屏过滤稳定耗时 34-36ms）
- [x] MutationObserver/过滤回调累计耗时 < 1s（2026-09-08：以长任务总量作上界代理——**0 个长任务、longtask 总量 0ms**；未做逐回调打点（需注入调试构建），但首屏 140 帖中 72 条判定+隐藏的整窗墙钟仅 ~3.7s 且无任何单帧阻塞，上界远低于 1s）
- [x] 悬浮条开启状态下滚动无额外卡顿（M3 节流生效）（2026-09-08：悬浮条开启 vs 关闭两组 fresh 内容对照，均为 0 长任务/60fps/0 慢帧，无可测差异；悬浮条可见性已确认）
- [x] 虚拟分页扫描进行中页面仍可交互（无长任务阻塞）（2026-09-08：个股 mock 页虚拟分页初始扫描（跨 2 原生页聚合）窗口内：**0 长任务**，事件循环滞后探针 36 样本**最大滞后 11ms**（250ms 节拍），60fps；扫描完成后「第 1 页 · 已扫描 2 个原始页」+ 原生列表隐藏正常）

---

## 13. 全新 Profile 端到端（P2-16 / P2 真机 #12，发布前闸门）——✅ 2026-09-09 通过（商店包真机，登出态边界如实标注）

步骤：全新 Chrome Profile → 加载商店包 ZIP。
- [x] 引导 3 步 → 默认关闭 → 雪球授权 → 粗粒度设置 → 过滤 → 关闭恢复 → 评论 → 失效模态全流程无报错（2026-09-09：全新 Profile（--user-data-dir 独立目录）加载 artifacts/timemachine-v1.0.0.zip → ①加载即自动弹 welcome 3 步引导（§1.1）→ ②未授权禁用态（§2.1）→ ③授权弹窗允许 → 截止=今日0点 → ④个股页 SZ300142 虚拟分页过滤生效（窗口判定正确）→ ⑤Popup 主开关「关闭过滤」→ 原生列表/分页即时恢复，重新「开启」→ 虚拟分页恢复（§4.3 关闭恢复链路）→ ⑥失效模态自然呈现（登出态首页 feed 路径零匹配 → 5s 模态：不可关闭 +「刷新重试」，即 §9.1 同款行为，见下方边界注记）→ ⑦§2.4 撤销授权 4 项。全流程**无运行时错误**——chrome://extensions 错误面板仅 6 条 Vite modulepreload 跨 world **警告**（CRXJS 构建产物通病，非运行时异常，已知无害）；SW 控制台仅启动日志）
- [x] 过程无任何网络请求指向非 jsDelivr/GitHub 的第三方（Network 面板核对，符合隐私承诺）（2026-09-09：SW DevTools Network 面板捕获扩展生命周期（重载/启动）网络活动——**仅 2 个请求且均为扩展自身模块**（service-worker chunk + adapters chunk），零第三方外呼；静态核查全库唯一 fetch() 调用指向 cdn.jsdelivr.net 占位地址（service-worker.ts:19），content script/popup/options 无任何网络调用。注：本次 reload 未触发 jsDelivr 拉取（onInstalled 仅 reason=install 时更新，重载 reason 不符，属预期），拉取行为本身归 §11 验证）

> 边界注记（如实记录）：本批全新 Profile 为**登出态**（无雪球账号），因此 ①登出态首页信息流帖子 DOM 不匹配 post_selectors，授权后 5s 触发已知 P3 误弹失效模态（与 09-08 观察一致，非商店包缺陷；模态行为本身即 §9.1 的自然呈现）；②登出态详情页评论 DOM 未被 comment_selectors 匹配（0 过滤/0 无法解析，评论过滤不生效）——**评论过滤的登录态验收维持 §7.1/7.2/7.4（dist-test 登录态）结论不变**，登出态覆盖差异登记为 P3 观察项（与 O1 同族：站点登出 DOM 变体）。上述两点不影响登录态目标用户场景；如需登出态全覆盖可安排用户在验证 Profile 登录后补测。

---

## 14. 发布准备核对（非功能测试，STORE-LISTING §6）——◐ 2026-09-08 部分完成

- [x] `npm run package` 生成剔除开发 key 的 ZIP（2026-09-08：`artifacts/timemachine-v1.0.0.zip`，27 条目；顺带修复打包脚本——PowerShell Compress-Archive 回退会产生反斜杠条目名（商店解包风险），新增 Python zipfile 回退（恒正斜杠）并重出包验证；manifest 核验：v1.0.0、无 key、无必授 host_permissions、5 个 optional）
- [x] manifest 版本已为 1.0.0（vite.config.ts 与 ZIP 内 manifest 一致）
- [x] 商店描述/关键词/分类文案就绪（`docs/STORE-LISTING.md`；2026-09-08 修正过时快捷键文案 Ctrl+Shift+T→Alt+Shift+T；「填写开发者控制台」为用户手动步骤）
- [ ] 隐私政策已托管为公开 URL（`docs/PRIVACY-POLICY.md` 内容就绪，权限表已同步 5 平台 optional origins；**托管到公开可访问 URL 待用户执行**——GitHub 仓库/raw/Gist 均可）
- [x] 截图 5-6 张（2026-09-08 已采集至 `store-assets/`，1280×800：引导页/虚拟分页/折叠效果/Popup/设置页/同花顺；其中虚拟分页与折叠两张为结构化 mock 页（真实雪球需登录态，自动化环境不可得），THS 一张无可见过滤痕迹（§10.1 未验收）——建议发布前登录态真机重截 2/3/6，详见 STORE-LISTING §4）
- [ ] VERIFY-CHECKLIST 本清单：全部 [x] 且失败项已闭环（截至 2026-09-08 尚有 §13、§2.1-2.4 商店包复验、§10.x 其他平台真机、§5.2 动态分组、§3.4/3.5/4.2/4.3/7.3、§6.8/6.10、§11 等未勾项）

---

## 历史已验证记录（供复验优先级参考）

| 日期 | 项 | 状态 |
|------|----|------|
| 2026-09-16/17 | dist-store 形态真机批（P2-20 + §11 + 过滤基线）：设置页多站矩阵（4 平台/授权状态/每站适配包版本+验证日期+能力摘要/集思录双域/页面类型能力/已记忆时间设置）✅；「授权全部金融站点」**一次弹窗**授予 5 origin（含 `www.jisilu.cn` + `jisilu.cn`）✅；per-site「移除」撤销即时停用（未刷新回原生形态）✅；截止对照实证过滤生效（今日0点→昨日帖可见；改昨日0点→昨日帖全部隐藏）✅；**§11 真机闭环**：`chrome.storage.local['adapters.remote']` = v1.0.0 远程包在存储中（拉取→校验→生效链路成立），`curl -I` 复核 CDN 可达；**新发现：远程包遮挡更新的内置包**（CDN 旧内容 last_verified=09-09 覆盖内置 v0.4.1，版本比较为「远程 vs 远程」）→ 记为运维纪律 + 待推送 CDN | ✅/⚠️ 详见 §2.3/§2.4/§2.5/§11 与 DEV-PLAN 批次十二 |
| 2026-09-16 | P2-17 切片 2 真机回归 + 雪球「加载更多」取证 + O1 区间复核 + P2-18 预设交易日验收：7x24 行选择器有效（`.timeline__live tr` 10→30 行全过滤）、关注/全部 15→30 行命中 16 条、屏数与末页文案真机捕获、`a.home__timeline__more` 全程隐藏（**推翻 09-09「滚动转按钮式」记录**，适配包按防御性声明 v0.4.1）、修正 `feed-backfill.ts` 无据注释、**发现重复补拉误判末页缺陷（本批已修 + 单测 2 项，真机复验待下批）**；区间=09:30–11:30 下视频类 8/8 全部过滤（**O1 正式关闭**）；「午间复盘」预设交易日正路径填值正确；探针用后整体移除；基线 17 文件全绿 | ✅/◐ 详见 §5.1/§5.6.1/§3.3 与 DEV-PLAN 批次十一（取消态与 10 屏上限本次未重测，取消态沿用 09-09 记录） |
| 2026-09-14 | O1 复核（视频卡片）：截止模式下 8/8 视频卡片命中 `article.timeline__item`、窗口外卡片 `display:none` → **O1 不可复现，不改适配包**；深色浮层探针取证法沉淀（雪球反调试 `debugger` 使 DevTools 路线不可用）；P2-17 切片 2 开发：load_more 按钮式获取（类型/schema/引擎/4 组测试）+ 扫描状态纳入脱敏诊断报告（shared/scan-text.ts，18 项测试）；`.kilo` 从工作树与全部历史清除；基线 17 文件全绿 | ✅/◐ 详见 §5.1 与 DEV-PLAN 批次十（雪球「加载更多」选择器取证、区间模式视频复核欠） |
| 2026-09-09 | 子代理代码审查（cc5a997+73c64dd 两提交）：**无 P1，可以合入**；2 个 P2 已修（options 渲染竞态 generation token+一次性替换、permissions 异常兜底+错误提示行，4e945aa），渲染冒烟顺延批次 E；P3×6（isGranted 部分授权态、日期合法性、周末预设 title、跨月测试用例、origin 清单同步、extractDomain 防御）登记未修 | ✅ 修复 详见 DEV-PLAN 批次八范围决策 |
| 2026-09-09 | 批次七+八（批次 C/D 开发）：P2-20 支持站点矩阵（授权状态/单站授权撤销/授权全部按钮/每站记忆管理）+ P2-18 页面类型能力矩阵（雪球 3 类页面 7 列表格）+ 金融区间预设 3 项（午间复盘/交易时段/交易日全天，工作日口径）+ last_verified 数据驱动验证日期；npm test 16 文件全绿（新增 sites-panel 27 项 + trading-day 13 项）、tsc 0 错、构建 ✅；dist-test 真机冒烟（设置页矩阵/区间 7 预设/交易日全天填值）通过；授权弹窗与新预设过滤行为回归攒批次 E | ✅ 开发完成 详见 DEV-PLAN 批次七/八 |
| 2026-09-09 | 批次六（批次B 闭闸门）：出新商店 ZIP（旧包留档 archive）→ 全新 Profile §13 端到端（引导/未授权态/授权/过滤/关闭恢复/失效模态/网络核验全过，登出态边界如实标注）→ §2.4 撤销授权 4 项（permissions.remove 生效，原生列表即时恢复，dist-test 机制限制正式解除）→ §2.1/2.2/2.5 同批闭环 → chrome://extensions 仅 6 条 modulepreload 警告（无害备案）→ **P2-16 闸门正式关闭** | ✅ 详见 §1.1/§2.1/§2.2/§2.4/§2.5/§13 与 DEV-PLAN 批次六记录 |
| 2026-09-09 | P2-17 切片 1 真机回归批：§5.6 信息流连续补拉全状态（白名单挂载/命中即停/exhausted/cancelled/区间模式/悬浮条屏数双语义）；§5.1 区间版其余类别抽查（自选/基金/资讯/达人/私募/ETF ✅，视频 ⚠️ 适配覆盖缺口——窗口外视频卡片绕过过滤链，新观察 O1）；个股页 SZ300142 回归（区间=今天虚拟分页激活/深扫 33 原生页手动取消/截止 09-06 切回即时重滤）；§0 基线四项复跑全绿（14 文件）；全程零风控 | ✅/⚠️ 详见 §5.1/§5.6 与 DEV-PLAN 09-09 批次记录 |
| 2026-09-08 | 商店包+性能+发布准备批：`npm run package` 出包（修复反斜杠条目缺陷）、§12 性能四项（mock 实测 0 长任务/60fps/扫描滞后≤11ms）、§14 截图 6 张 + 文案修正（Alt+Shift+T）+ 隐私政策权限表 5 平台 | ✅/◐ 详见各节（隐私政策托管 URL 与控制台填写为用户步骤） |
| 2026-09-08 | P3 新观察：未登录访问雪球首页会误弹失效模态（未登录 DOM 无 post_selectors 匹配、feed 路径 ^/$ 命中）；打包脚本回退链曾产出反斜杠条目 ZIP（已修）；docs/ 下残留一份旧 timemachine-v1.0.0.zip（早于本次产物） | ℹ️ 见 DEV-PLAN 交接记录 |
| 2026-09-08 | P2-16 收尾批：§9.1 失效模态（篡改 post_selectors 构造）、§9.4/9.5 诊断明细与报告脱敏（篡改 timestamp.selector 构造，剪贴板 powershell 核验）、§7.4 详情评论区间双向（单页双向样本+关/开对照）、§5.1 关注/热门区间抽查、§0 基线复跑（13 文件）；全程零风控（dist-test，低频会话） | ✅ 详见各节 |
| 2026-09-08 | 环境实况：日常 Profile 中 dev 时光机（eobbppih）不存在（搜索仅命中 test），双扩展污染风险不存在；详情页角标在懒加载评论批次被过滤时未即时增长（Popup 计数正确=80），低严重度观察项待核 | ℹ️ 见 DEV-PLAN 交接记录 |
| 2026-09-07 | P2-15/16 批：§3.2 区间校验、§3.3 预设、§3.6 双 Tab 同步、§5.1-5.5 首页十类/7x24/二级/标注/三屏、§6.2 聚合、§6.6 取消扫描（59 页）、§6.7a 改时间、§6.9 切换重建、§4.6 悬浮条、§6.5「原始页面加载失败」终态（dist-test，低频会话；区间深回溯一次触发风控滑块，人工通过后续测） | ✅ 详见各节 |
| 2026-09-07 | §8 快捷键 Ctrl+Shift+T 被 Chrome 内置「重开标签页」抢占未生效（需改绑）；§9.4/9.5 因 0 无法解析未构造样本；§6.7b 关过滤同受阻；§6.5 末页/上限未走到（深度不足，自动化覆盖） | ⚠️ 详见各节 |
| 2026-09-06 | §0 基线四项、§1.4 重载注入、§6.1 六类别（含交易）、§6.3 去重、§6.4 缓存、§7.2 评论分钟级判定、§9.2/9.3 模态不误报（dist-test，沃森生物+首页+详情页，低频会话零风控） | ✅ 详见各节 |
| 2026-09-06 | §2.4 撤销授权：dist-test 上机制性不可执行（manifest 必授权限不可 remove，Chrome 错误页取证）；H2 自动化已覆盖，待商店包构建复验 | ⚠️ 见 §2.4 |
| 2026-08-13 | 用户确认：雪球截止过滤基础功能正常（早期构建） | ✅ |
| 2026-08-13 | 虚拟分页：第一页 10 条、状态显示、原生隐藏、空类别、返回恢复（部分构建） | ✅ 建议按本清单 6.x 复验最新构建 |
| 2026-08-17 | 详情评论区间双向样本（08-11 保留 / 08-13+今天 8 条隐藏） | ✅ 供回归 |
| 2026-08-17 | 首页普通类别、7x24、个股分页第一页/下一页/上一页缓存（更早构建） | ✅ 最新构建待复验 |
| 2026-08-17 | 「交易」类别测试触发雪球风控滑块，结果无效，需重测 | ⚠️ 见 6.1 |

---

## 风控纪律（每次测试前必读）

1. 连续操作不超过 2-3 次翻页/切换，每次间隔 ≥ 3s；
2. 出现「访问验证」滑块 → 立即停止自动操作，手动通过后休息 5 分钟；
3. 真机验证与自动化测试分开时段进行，不在同一会话连续触发；
4. 每次切换类别/页面后等待原生列表稳定再截图/断言。
