# 时光机 — 手动测试 / 真机验收清单

> 前置：Windows 本机 Chrome；加载 `npm run test:build` 产物（`dist-test/`）或打包 ZIP；雪球/东财建议用已登录的日常 Profile（登录态下东财系无滑块）。
> 状态标记：`[x]` 通过；`[ ]` 待测。**已通过项只保留一行结论，过程证据见 git 历史**（`git log -p VERIFY-CHECKLIST.md`）；只有未通过项展开操作步骤。
> 文档定位：产品设计权威 `PRD-v1.0.md`；任务与进度 `DEV-PLAN.md`。

## 怎么读这份清单（新手导览）

### 黑话翻译表

| 文档里的词 | 大白话意思 |
|---|---|
| 扩展 / 插件 | 装在 Chrome 里的小工具，就是"时光机" |
| Popup（弹出面板） | 点浏览器右上角时光机图标后弹出的小控制面板 |
| 图标变灰 / 彩色 | 灰色 = 过滤关闭；彩色 = 过滤开启 |
| 角标（badge） | 图标右下角的小数字，表示本页已隐藏多少条帖子 |
| Profile | Chrome 的一套独立环境（独立登录与插件）。"全新 Profile" = 干净、未登录的浏览器环境 |
| 截止模式 | 设一个时间点 T，把 T 之后发布的帖子藏起来 |
| 区间模式 | 设开始～结束两个时间点，只保留区间内的帖子 |
| 隐藏策略 | 被过滤的帖子直接消失 |
| 折叠策略 | 被过滤的帖子变成「⏳ 此帖被时光机过滤」小条子，点开可看原文 |
| 授权 | 首次使用时 Chrome 询问是否允许插件访问某网站，点"允许" |
| 注入 | 插件把代码放进网页里干活；没注入 = 过滤不生效 |
| 重载扩展 | `chrome://extensions` 页面上时光机卡片的圆形箭头按钮 |
| display:none | 网页术语"彻底隐藏"；肉眼看到帖子消失即是 |
| 悬浮提示条 | 页面右下角的半透明深色框，实时显示过滤情况（需在设置页开启） |
| 虚拟分页 | 个股页上插件自造的「第 1 页 / 下一页」控件（代替网站原生分页） |
| 原生列表 / 原生分页 | 网站自带的（非插件生成）列表与翻页条 |
| 适配包 | 一份"说明书"，告诉插件在每个网站怎么找帖子、怎么读时间 |
| 失效模态 | 网站改版导致找不到帖子时，屏幕中央弹出「页面结构可能已变化」，只能「刷新重试」 |
| 无法解析 | 时间插件看不懂的帖子，默认正常显示（不会误删） |
| 风控 / 访问验证滑块 | 雪球反作弊：操作太快弹滑块，触发后本次结果作废 |
| 误杀 / 漏杀 | 误杀 = 该显示的被藏了；漏杀 = 该藏的还在显示 |

### 测试前的准备

1. 确认代码：`npm test` 全绿、`npx tsc --noEmit` 无输出、`rm -rf dist && npm run build` 成功、`npm run test:build` 生成 `dist-test/`。
2. 装到 Chrome：Windows 本机 `chrome://extensions` → 开发者模式 → 「加载已解压的扩展程序」选择 `dist-test/`（或打包 ZIP）。图标出现、点开无红色报错即通过。
3. 纪律：用已登录雪球的日常 Profile；动作要慢，连续翻页/切换 ≤2~3 次、间隔 ≥3 秒；弹「访问验证」滑块立即停止、手动通过后休息 5 分钟（该步结果作废）。详见文末「风控纪律」。
4. 记录：通过打 `[x]`；失败写现象（附截图 + F12 控制台红字）。

## 0. 测试前基线

- [x] 2026-09-09：`npm test` 17 文件全绿、`npx tsc --noEmit` 0 错、干净构建（loader 校验通过）、`dist-test/` 加载无报错。

## 1. 安装、引导与基础状态

- [x] 1.1 首次安装 3 步引导可走完，完成后默认关闭（2026-09-09，商店包全新 Profile）
- [x] 1.4 扩展重载后注入稳定，无需手动开 Popup（2026-09-06 复验）
- [ ] 1.2 扩展更新不弹引导页（M2）：改 `vite.config.ts` version 重新构建加载，更新后**不**自动打开 welcome.html
- [ ] 1.3 引导结果写入 `prefs.defaultStrategy` 与 `prefs.onboarded`

## 2. 授权模型（按需授权）

- [x] 2.1 全新 Profile 未授权态：Popup 提示未授权，主开关与时间控件禁用（2026-09-09）
- [x] 2.2 授权弹窗允许后控件启用，刷新页面过滤生效（2026-09-09）
- [x] 2.3 jisilu 双域一次授权授予，无死结（2026-09-16）
- [x] 2.4 撤销授权即时停用：隐藏帖恢复、角标清空、滚动新帖不再过滤、刷新后无痕迹（2026-09-09 商店包；2026-09-16 新 UI 路径复核）
- [x] 2.5 设置页授权列表与移除（2026-09-09；2026-09-16 四平台矩阵）
- [ ] 2.6 非目标站点禁用时间控件（M4）：github.com 打开 Popup → 显示「未知站点」、控件全禁用、`chrome.storage.local` 不新增 `timeSettings.github.com`

## 3. 时间设置与 Popup 控制面板

- [x] 3.2 区间模式与校验（开始晚于结束报错且不覆盖旧值；只填一端报错）（2026-09-07）
- [x] 3.3 预设：截止 6 项 + 区间 7 项（含金融预设与交易日正路径）（2026-09-07/09/16）
- [x] 3.5 设置持久化：跨扩展重载与浏览器会话回显一致（2026-09-08）
- [x] 3.6 跨 Tab 同步：A 改时间 B 自动重滤（2026-09-07）
- [ ] 3.1 截止模式：datetime-local 分钟级、保存后回显一致、默认策略随全局 prefs
- [ ] 3.4 边界判定（零误杀/零漏杀）：恰等于 T 的帖子显示、T+1 分钟隐藏；抽查 10 条与页面显示时间对照

## 4. 过滤执行与策略

- [x] 4.6 悬浮提示条：内容含边界与计数、滚动不闪烁、× 可关、设置页关闭后不再注入（2026-09-06/07）
- [ ] 4.1 隐藏策略：新帖 `display:none`、老帖保留、页面无错位
- [ ] 4.2 折叠策略：占位条渲染、点击展开、再次折叠、展开后布局与站点原生一致（M5）
- [ ] 4.3 关闭/开启恢复：全部恢复（含折叠展开态）、计数归零后重新累计
  - 隐藏链路与虚拟分页形态已通过（2026-09-08/09）；**折叠展开态待测**
- [ ] 4.4 无法解析回退：默认显示 + 标注「N 条无法解析」
- [ ] 4.5 计数与角标：随滚动实时变化（200ms 内）、切换/关闭后重置
  - 多批次观察通过；**200ms 阈值未计时**；懒加载评论分批过滤时角标未即时增长（低严重度观察项）

## 5. 雪球首页类别信息流（P2-14 / P2-16）

- [x] 5.1 一级十类切换、计数隔离、截止与区间两模式（2026-09-07/08/09；视频类 O1 于 09-14/16 复核结案）
- [x] 5.2 二级类别：关注流「全部/关注精选/只看原发/未分组」、自选流「全部/公告/新闻」各自独立（2026-09-07）
- [x] 5.3 7x24 分组标题 + 行内 `HH:mm` 组合解析，分钟级边界正确（2026-09-07）
- [x] 5.4 智能/热度流标注「仅过滤已加载内容」（2026-09-07）
- [x] 5.5 连续滚动 3 屏无重复帖、无计数错乱、无旧上下文串流（2026-09-07）
- [x] 5.6 信息流连续补拉：白名单挂载、命中即停、末页/取消、区间模式、屏数双语义（切片 1 2026-09-09；切片 2 2026-09-16）
  - 切片 2 未重测：取消态、10 屏上限（站点早停滞）；已修「已在底部重复触发误判末页」待真机复验
- [ ] 5.7 动态自定义分组出现时补测（P2-14 遗留）

## 6. 雪球个股页虚拟分页（P2-12 / P2-16）

- [x] 6.1 全部/讨论/投资者关系/交易/资讯/公告六类各自虚拟第 1 页 10 条、原生列表分页隐藏（2026-09-06）
- [x] 6.2 首屏跨页聚合，激进截止不空屏（2026-09-07）
- [x] 6.3 「下一页」才续扫，第二页与第一页无重复 ID（2026-09-06）
- [x] 6.4 「上一页」走缓存，不增加已扫描原生页数（2026-09-06）
- [x] 6.5 终态提示：加载失败文案真机；「已到末页/已达上限」由自动化覆盖（2026-09-07）
- [x] 6.6 扫描中「取消扫描」保留已收集虚拟页（2026-09-07）
- [x] 6.7 扫描中改截止时间/关闭过滤 → 旧扫描停止并恢复原生列表（2026-09-07/08）
- [x] 6.9 类别来回切换：从第 1 页重新聚合、缓存隔离（2026-09-07）
- [ ] 6.8 空类别：保留原生「该股票暂无信息」、无虚拟控件无报错；返回「讨论」后恢复正常
- [ ] 6.10 原生隐藏抵抗：站点异步改回 `display:block` 时 1s 内重新隐藏

## 7. 详情页评论过滤（P2-4 / P2-16）

- [x] 7.1 评论按自身时间戳独立判定：老帖新评论被隐藏、正文保留（2026-09-06/07）
- [x] 7.2 「今天 HH:mm」按当日时刻判定，分钟级双向验证（2026-09-06）
- [x] 7.4 区间模式评论双向样本（区间内保留/区间外隐藏 + 关开对照）（2026-09-08）
- [ ] 7.3 无时间戳评论回退：策略=全部显示 / 默认折叠，切换后行为立即更新

## 8. 快捷键（P2-7）

- [x] `Alt+Shift+T` 切换当前页过滤开关，图标同步；未设时间/未授权页面无效果（M6）（2026-09-08）
- 运维：suggested_key 变更后重载/更新**不会**自动应用绑定，需在 `chrome://extensions/shortcuts` 手动绑定一次；捕获框不接收合成按键

## 9. 失效检测、诊断与隐私

- [x] 9.1 篡改 `post_selectors` 零匹配 → 5s 内不可关闭模态，仅「刷新重试」出口（2026-09-08 构造法）
- [x] 9.2 个人主页/搜索页不误报模态（2026-09-06）
- [x] 9.3 详情评论页不误报模态（2026-09-06）
- [x] 9.4 诊断明细含 {kind, page, context, raw} 样本（≤8 条），无法解析帖默认显示（2026-09-08）
- [x] 9.5 诊断报告脱敏：仅平台/版本/边界/计数/pathname/时间原文，无 query/正文/账号/Cookie（2026-09-08）

## 10. 其他平台适配（均欠真机）

- [ ] 10.1 同花顺 `t.10jqka.com.cn`：首页按 MMDD+HH:mm 过滤；跨年帖回退一年、缺年份补当年；个股吧等页面静默无异常
- [ ] 10.2 集思录 `jisilu.cn`：分类列表按「作者 回复 • YYYY-MM-DD HH:mm」过滤；详情回复按各自时间；大帖分页/懒加载机制观察；首页 explore 静默退出
- [ ] 10.3 东方财富资讯 `finance.eastmoney.com`：`/a/` 栏目列表按中文时间过滤；`_{n}.html` 分页逐页可过滤；正文页静默退出

## 11. 适配包热更新（P2-5 / P2-19）

- [x] 安装时拉取 → SHA-256 → schema → 写入存储 → 生效，真机闭环；CDN 可达（2026-09-16/17）
- [x] 发布源同步 `adapters.json` v1.0.1，三方 SHA-256 一致（2026-09-17）
- [ ] 设置页开关关闭后不再请求（自动化已覆盖 `disabled` 短路）
- [ ] 完整性校验：`.sha256` 不匹配时丢弃且不覆盖已存远程包（自动化已覆盖 2026-09-09）
- [ ] 应急停用：远程包 `disabled: true` → 回退内置 + 广播（自动化已覆盖 2026-09-09）
- [ ] 远程版本递增的设备侧落地：等下一个 12h alarm 周期后读 `adapters.remote.version`
- 纪律：远程包会遮挡更新的内置包 → 改内置包后必须 `npm run adapters:build` → 提交 → 推送 → purge CDN（详见 `AGENTS.md`）

## 12. 性能（P1-4）

- [x] 2026-09-08：80 帖首屏 + 3 屏滚动，0 长任务 / 60fps / 0 慢帧；悬浮条无额外卡顿；虚拟分页扫描中事件循环滞后 ≤11ms（playwright mock 页实测，未在登录态真站复测）

## 13. 全新 Profile 端到端（发布前闸门）

- [x] 2026-09-09：商店包全新 Profile 走完引导→授权→过滤→关闭恢复→评论→失效模态，无运行时错误；Network 仅 jsDelivr/GitHub（登出态边界如实标注，评论过滤登录态结论见 §7）

## 14. 发布准备核对（非功能）

- [x] `npm run package` 出无 key ZIP（正斜杠条目）；manifest 1.0.0；商店描述/关键词/分类就绪；截图 6 张（2/3/6 建议登录态重截）
- [ ] 隐私政策托管为公开 URL（用户执行）
- [ ] 本清单全部 `[x]` 且失败项闭环

## 风控纪律（每次测试前必读）

1. 连续操作不超过 2-3 次翻页/切换，每次间隔 ≥ 3s；
2. 出现「访问验证」滑块 → 立即停止自动操作，手动通过后休息 5 分钟；
3. 真机验证与自动化测试分开时段进行，不在同一会话连续触发；
4. 每次切换类别/页面后等待原生列表稳定再截图/断言。
