# 时光机 — 手动测试 / 真机验收清单

> 版本：2026-08-19｜覆盖：v1.0 全部 P0/P1 功能 + 2026-08-19 深度 Review 修复批次的复核点（标注「Hx/Mx 新行为」）。
> 前置条件：**Windows 本机 Chrome**（`scripts/e2e.mjs` 硬编码 Windows 路径）；已加载 `npm run test:build` 产物（`dist-test/`）或打包 ZIP；雪球/东财建议使用**已登录的日常 Profile**（登录态下东财系无滑块）。
> 风控提示：雪球连续自动翻页/快速点击会触发「访问验证」滑块，污染测试结果。**每次切换类别/页面后等原生列表稳定，降低操作频率**；触发风控即停止测试、休息后再继续。
> 状态标记：`[x]` 通过；`[ ]` 待测；失败时在条目后注明现象（截图/console 报错）。

---

## 0. 测试前基线

- [ ] `npm test` 12 个文件全绿
- [ ] `npx tsc --noEmit` 0 错误
- [ ] `rm -rf dist && npm run build` 成功
- [ ] `npm run test:build` 生成 dist-test/ 并在 chrome://extensions 加载成功、图标正常无报错

---

## 1. 安装、引导与基础状态

### 1.1 首次安装引导页
步骤：全新 Profile（或卸载重装）→ 加载扩展。
- [ ] 自动打开 welcome.html，3 步引导可走完
- [ ] 引导完成后插件默认关闭（图标灰色）

### 1.2 扩展更新不弹引导页（M2 新行为）
步骤：已有 Profile 上更新扩展版本（改 vite.config.ts version 重新构建加载）。
- [ ] 更新后**不**自动打开 welcome.html（此前版本会弹，M2 修复）

### 1.3 引导写入 prefs
步骤：完成引导 → chrome.storage.local 检查。
- [ ] `prefs.defaultStrategy` 与 `prefs.onboarded` 已写入

### 1.4 扩展重载后的注入稳定性（已知问题）
步骤：重载扩展 → 新开雪球标签 → 打开 Popup。
- [ ] 能记录现象；若未注入，打开 Popup 触发注册同步后刷新可恢复（已知缓解路径）
- [ ] `chrome://extensions` service worker 控制台无未捕获异常

---

## 2. 授权模型（按需授权）

### 2.1 全新 Profile 首次访问（P2 真机 #6）
步骤：新 Profile 打开雪球 → 点插件图标。
- [ ] Popup 显示「此站点未授权」+「授权此站点」按钮，开关禁用，时间控件禁用
- [ ] 授权前预设按钮存在但禁用

### 2.2 授权流程
步骤：点击「授权此站点」→ 授权弹窗允许。
- [ ] 授权成功后授权区隐藏、时间控件启用、过滤脚本注入当前页
- [ ] 页面刷新后过滤生效（新帖隐藏/老帖保留）

### 2.3 www 子域变体授权（jisilu）
步骤：访问 `www.jisilu.cn` → Popup 授权。
- [ ] 一次请求可同时授予 `www.jisilu.cn` 与 `jisilu.cn` 两个 origin（无授权死结）

### 2.4 撤销授权立即停用（H2 新行为）
步骤：开启过滤（有隐藏帖子）→ 设置页「已授权站点管理」移除 xueqiu.com → 返回雪球页面（**不刷新**）。
- [ ] 已隐藏/折叠的帖子即时恢复显示、占位条消失、悬浮条消失
- [ ] 图标立即变灰、角标清空
- [ ] 之后滚动加载的新帖**不再**被过滤（观察器已断开）
- [ ] 刷新页面后确认无过滤效果、无报错

### 2.5 设置页授权管理（P2 真机 #7）
- [ ] 设置页可列出已授权站点（*:// 条目）
- [ ] 点击「移除」后站点从列表消失（与 2.4 联动）

### 2.6 非目标站点禁用时间控件（M4 新行为）
步骤：打开 Popup 在 github.com 等无适配包页面。
- [ ] 站点显示「未知站点」、时间输入/预设/模式切换全部禁用
- [ ] 无法对非目标域名写入时间设置（chrome.storage.local 无新增 `timeSettings.github.com`）

---

## 3. 时间设置与 Popup 控制面板

### 3.1 截止模式
- [ ] datetime-local 输入可设置分钟级时间，保存后回显一致
- [ ] 默认策略随全局 prefs 同步显示

### 3.2 区间模式与校验（P2 真机 #P2-13）
- [ ] 切「区间」→ 起止输入出现；保存后回显
- [ ] 开始时间晚于结束时间 → 报错「开始时间不能晚于结束时间」，不覆盖上次有效设置
- [ ] 只填一端 → 报错「请同时设置开始时间和结束时间」

### 3.3 快捷预设
- [ ] 雪球 6 项截止预设按钮齐全：「1小时前 / 今日0点 / 昨日0点 / 今天09:15 / 今天09:30 / 昨日收盘前」
- [ ] 每项点击后输入框值正确、保存生效
- [ ] 区间 4 项预设（今天/昨天/近3天/本周）起止值正确

### 3.4 边界判定（零误杀/零漏杀实机抽查）
步骤：截止设为 T，找一条时间恰好=T 的帖子与一条 T+1 分钟的帖子。
- [ ] 恰好等于 T 的帖子显示，T 之后 1 分钟隐藏
- [ ] 抽查 10 条，过滤前后与期望一致（对照页面显示时间）

### 3.5 设置持久化（P1 真机 #5）
- [ ] 关闭面板、刷新页面后截止/区间时间保持

### 3.6 跨 Tab 同步（P1 真机 #9）
步骤：A、B 双 Tab 打开雪球 → A 面板改时间。
- [ ] B Tab 无需操作自动按新时间重新过滤（storage.onChanged → 广播）

---

## 4. 过滤执行与策略

### 4.1 隐藏策略（P1 真机 #4）
步骤：雪球首页设「1小时前」→ 开启。
- [ ] 新帖隐藏（display:none）、老帖保留，页面无错位

### 4.2 折叠策略（P1 真机 #6）
- [ ] 切换折叠后过滤帖变为「⏳ 此帖被时光机过滤」占位条
- [ ] 点击占位条帖子展开、占位条移除；再次设置后重新折叠
- [ ] 展开恢复的帖子布局与站点原生一致（M5：原 inline display 被还原，无布局跳动）

### 4.3 关闭/开启恢复（P1 真机 #7）
- [ ] 关闭过滤 → 全部恢复显示（含折叠展开态）；重新开启 → 再次过滤
- [ ] 计数归零/恢复正确

### 4.4 无法解析回退
- [ ] 无法解析的帖子默认显示，Popup 与悬浮条标注「N 条无法解析」

### 4.5 计数与角标（P1 真机 #8）
- [ ] Popup「已过滤 N 条」与图标角标随滚动实时变化（200ms 内可达最终值）
- [ ] 切换时间/关闭过滤后计数重置

### 4.6 悬浮提示条（P2 真机 #9）
- [ ] 设置页开启 → 页面右下角出现，内容含边界时间与已过滤数
- [ ] 滚动加载时内容不闪烁/不抖动（M3 节流后每 250ms 至多更新一次）
- [ ] × 关闭后消失；设置页关闭后不再注入

---

## 5. 雪球首页类别信息流（P2-14 / P2-16）

### 5.1 一级类别切换
步骤：登录态首页，逐一切换 关注/热门/7x24/自选/视频/基金/资讯/达人/私募/ETF。
- [ ] 每类切换后过滤按当前时间设置生效
- [ ] 切换时旧类别扫描/观察器取消，无旧上下文串流
- [ ] 每类计数隔离（切类后从该类别自身计数开始）

### 5.2 二级类别
- [ ] 关注流：「全部/关注精选/只看原发/未分组」各自独立上下文
- [ ] 自选流：「全部/公告/新闻」各自独立上下文
- [ ] 动态自定义分组出现时补测（P2-14 遗留）

### 5.3 7x24 时间解析
- [ ] 「今天」/「昨天」分组标题 + 行内 HH:mm 组合正确；今日快讯隐藏、昨日保留（对照截止时间）

### 5.4 智能流完整性标注（P2-15）
- [ ] 首页普通类别 Popup 显示「当前类别为智能或热度信息流，仅过滤已加载内容」
- [ ] 悬浮条含「仅过滤已加载内容」

### 5.5 连续滚动 3 屏（P2-16）
- [ ] 滚动追加 3 屏无重复帖子被重复计数、无计数错乱、无旧上下文内容混入

---

## 6. 雪球个股页虚拟分页（P2-12 / P2-16 / P2 真机 #13-14）

> 风控重点：每步之间等待原生列表稳定，翻页频率放缓。

### 6.1 六类别全量终验（P2-16，08-17 记录「交易」类别受风控污染未终验）
- [ ] 全部 / 讨论 / 投资者关系 / 交易 / 资讯 / 公告 六类各自：虚拟第 1 页 10 条、「第 1 页 · 已扫描 N 个原始页」、原生 `.status-list`/`.pagination` 隐藏

### 6.2 首屏聚合（P2 真机 #13）
- [ ] 从原始第 1 页开始按需扫描，前 10 条符合时间的帖子组成虚拟第 1 页即停止
- [ ] 首屏全空场景（截止激进）不出现空白：跨页聚合出内容并显示扫描页数

### 6.3 下一页续扫与去重（08-17 记录待最新构建复验）
- [ ] 点击「下一页」才继续扫描（不预取超过所需页）
- [ ] 第二页与第一页无重复 ID（data-id 去重生效）

### 6.4 上一页缓存（08-17 记录待复验）
- [ ] 「上一页」直接出缓存，不增加已扫描原生页数、不重新请求

### 6.5 终态提示
- [ ] 到达末页显示「已到末页」；达到 200 页显示「已达上限」；原生页加载失败显示「原始页面加载失败」

### 6.6 扫描中取消（P2 真机 #14）
- [ ] 扫描中「取消扫描」→ 状态变「扫描已取消」，页面保留已收集的虚拟页

### 6.7 扫描中修改时间/关闭（P2 真机 #14）
- [ ] 扫描中修改截止时间 → 旧扫描停止，按新边界重新聚合
- [ ] 扫描中关闭过滤 → 虚拟控件移除，原生列表/分页恢复显示

### 6.8 空类别（08-17 已通过，供回归）
- [ ] 「投资者关系」无内容时保留原生「该股票暂无信息」，无虚拟控件、无报错
- [ ] 从空类别返回「讨论」→ 虚拟分页恢复正常

### 6.9 类别切换重建（P2-12）
- [ ] 六类之间来回切换：旧会话取消、新类别从第 1 页重新聚合、缓存隔离

### 6.10 原生隐藏抵抗
- [ ] 扫描期间原生列表/分页被站点异步改回 display:block 时，1 秒内重新隐藏（属性观察器）

---

## 7. 详情页评论过滤（P2-4 / P2-16 / P2 真机 #3-4）

### 7.1 评论独立判定
- [ ] 评论按自身时间戳与截止/区间独立判定（老帖新评论被隐藏，保持时间隔离感）

### 7.2 「今天/昨天 HH:mm」评论（P2 真机 #3 + H1 复核）
步骤：找一条「昨天 HH:mm · 省份」评论，截止设为该帖时刻之后（应保留）/ 之前（应隐藏）。
- [ ] 「昨天 HH:mm」按具体时刻判定（H1 修复后不得出现小时级漂移）
- [ ] 「今天 HH:mm」按当日时刻判定

### 7.3 评论无时间戳回退（P2 真机 #4）
- [ ] 回退策略=全部显示：无时间戳评论显示、计数计入「无法解析」
- [ ] 回退策略=默认折叠：无时间戳评论折叠为占位条
- [ ] 切换策略后行为立即更新

### 7.4 区间模式评论双向样本（08-17 已通过，供回归）
- [ ] 区间内评论保留、区间外（含「今天」）隐藏

---

## 8. 快捷键（P2-7，待 Chrome 实测）

- [ ] `Ctrl+Shift+T` 切换当前标签页过滤开关，图标彩色/灰色同步
- [ ] 未设定时间/未授权页面按快捷键无效果、图标状态不变（M6 守卫）

---

## 9. 失效检测、诊断与隐私

### 9.1 失效模态（P2 真机 #8 / P1-5）
步骤：devtools 篡改适配包 `post_selectors` 使其零匹配（或临时改配置）→ 首页刷新。
- [ ] 5s 内出现不可关闭「页面结构可能已变化」模态，按钮「刷新重试」

### 9.2 个人主页/搜索页不误报（H4 新行为）
步骤：访问 xueqiu.com 某用户主页、搜索结果页，等待 6s。
- [ ] 无失效模态、无任何注入痕迹，页面不受影响

### 9.3 详情评论页不误报（H4 新行为）
步骤：打开帖子详情页（正文 + 评论结构），等待 6s。
- [ ] 无失效模态（此前版本可能误报）

### 9.4 诊断明细
- [ ] Popup 展开「查看无法解析的时间」能看到 {kind, page, context, raw} 样本（≤8 条）

### 9.5 诊断报告脱敏（P2-19 部分）
- [ ] 「复制诊断报告」仅含平台/适配包版本/模式边界/计数与 pathname+时间原文
- [ ] 报告不含 URL query、帖子正文、账号、Cookie

---

## 10. 其他平台适配（P2 真机 #1-2 / 2026-08-18 欠账）

### 10.1 同花顺 t.10jqka.com.cn
- [ ] 首页信息流授权后按 MMDD（data-date）+ HH:mm 组合时间过滤
- [ ] 跨年帖正确回退一年；缺年份补当年
- [ ] 个股吧/圈子页面无适配（产品线废弃），静默无异常

### 10.2 集思录 jisilu.cn（08-18 内置包，真机验收欠）
- [ ] 分类列表（/category/4 等）按「作者 回复 • YYYY-MM-DD HH:mm」解析过滤
- [ ] 详情回复（/question/{id}）按各自时间过滤
- [ ] 大帖回复分页/懒加载机制观察记录（recon 遗留）
- [ ] 首页 explore（无时间戳路径）静默退出、无失效模态

### 10.3 东方财富资讯 finance.eastmoney.com（08-18 内置包，真机验收欠）
- [ ] `/a/czqyw.html` 与 `/a/cgspl.html` 列表按 `YYYY年MM月DD日 HH:mm` 过滤
- [ ] `_{n}.html` 分页逐页可过滤
- [ ] 正文页（/a/数字.html）静默退出（active_paths 白名单外）

---

## 11. 适配包热更新（P2-5 / P2 真机 #10）

> 真实发布源尚未建立（REMOTE_ADAPTERS_URL 为占位地址）；此节仅验证机制本身。

- [ ] 安装时触发一次拉取；网络失败/404 时静默降级内置包（SW 控制台看日志，无用户可见报错）
- [ ] 设置页开关关闭后不再请求
- [ ] （mock 环境已覆盖：远程版本更高 → 存储更新 + 广播 + 内容脚本重扫；真机可配合本地 HTTP 服务验证一次完整链路）

---

## 12. 性能（P1 真机 #10，P1-4）

步骤：雪球首页含 50-80 帖、连续滚动 3 屏，DevTools Performance 录制。
- [ ] 无明显卡顿
- [ ] MutationObserver/过滤回调累计耗时 < 1s
- [ ] 悬浮条开启状态下滚动无额外卡顿（M3 节流生效）
- [ ] 虚拟分页扫描进行中页面仍可交互（无长任务阻塞）

---

## 13. 全新 Profile 端到端（P2-16 / P2 真机 #12，发布前闸门）

步骤：全新 Chrome Profile → 加载商店包 ZIP。
- [ ] 引导 3 步 → 默认关闭 → 雪球授权 → 粗粒度设置 → 过滤 → 关闭恢复 → 评论 → 失效模态全流程无报错
- [ ] 过程无任何网络请求指向非 jsDelivr/GitHub 的第三方（Network 面板核对，符合隐私承诺）

---

## 14. 发布准备核对（非功能测试，STORE-LISTING §6）

- [ ] `npm run package` 生成剔除开发 key 的 ZIP
- [ ] manifest 版本已为 1.0.0
- [ ] 商店描述/关键词/分类填写完毕
- [ ] 隐私政策已托管为公开 URL
- [ ] 截图 5-6 张（安装引导、虚拟分页、隐藏/折叠、设置页、同花顺）
- [ ] VERIFY-CHECKLIST 本清单：全部 [x] 且失败项已闭环

---

## 历史已验证记录（供复验优先级参考）

| 日期 | 项 | 状态 |
|------|----|------|
| 2026-08-13 | 用户确认：雪球截止过滤基础功能正常（早期构建） | ✅ |
| 2026-08-13 | 虚拟分页：第一页 10 条、状态显示、原生隐藏、空类别、返回恢复（部分构建） | ✅ 建议按本清单 6.x 复验最新构建 |
| 2026-08-17 | 详情评论区间双向样本（08-11 保留 / 08-13+今天 8 条隐藏） | ✅ 供回归 |
| 2026-08-17 | 首页普通类别、7x24、个股分页第一页/下一页/上一页缓存（更早构建） | ✅ 最新构建待复验 |
| 2026-08-17 | 「交易」类别测试触发雪球风控滑块，结果无效，需重测 | ⚠️ 见 6.1 |

---

## 风控纪律（每次测试前必读）

1. 连续操作不超过 2-3 次翻页/切换，每次间隔 ≥ 3s；
2. 出现「访问验证」滑块 → 立即停止自动操作，手动通过后休息 5 分钟；
3. 真机验证与自动化测试分开时段进行，不在同一会话连续触发；
4. 每次切换类别/页面后等待原生列表稳定再截图/断言。
