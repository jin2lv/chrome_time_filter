# 时光机 — 手动测试新手指南

> 这份文档是 `VERIFY-CHECKLIST.md` 和 `DEV-PLAN.md` 中测试内容的"大白话版"。
> 你不需要懂任何前端知识，照着一步步点鼠标就行。
> 每个测试项都写清楚了：**怎么操作 → 应该看到什么**。

---

## 一、先认识几个词（黑话翻译表）

| 文档里的词 | 大白话意思 |
|---|---|
| **扩展 / 插件 / 扩展程序** | 装在 Chrome 浏览器里的小工具，就是本项目做的"时光机" |
| **Popup（弹出面板）** | 点浏览器右上角的时光机图标后，弹出来的那个小控制面板 |
| **图标变灰 / 彩色** | 插件图标的样子：灰色 = 过滤关闭；彩色 = 过滤开启 |
| **角标（badge）** | 图标右下角显示的小数字，表示"这个页面上已经隐藏了多少条帖子" |
| **Profile（用户配置文件）** | Chrome 的一套独立环境，各自有独立的登录状态和插件。右上角头像处可切换/新建。"全新 Profile" = 一个干净没登录过的浏览器环境 |
| **雪球** | xueqiu.com 网站（主要测试对象） |
| **帖子 / 状态** | 雪球页面上的每一条发言，类似微博的一条动态 |
| **截止模式** | 设一个时间点 T，把 T 之后发布的帖子藏起来（只看 T 之前的世界） |
| **区间模式** | 设两个时间点（开始～结束），只保留这段时间内的帖子，两头之外的都藏起来 |
| **隐藏策略** | 被过滤的帖子直接消失不见 |
| **折叠策略** | 被过滤的帖子不消失，变成一个写着「⏳ 此帖被时光机过滤」的小条子，点它可以展开看原文 |
| **授权** | 第一次使用时，Chrome 会问你是否允许插件访问某个网站，点"允许"即可 |
| **注入** | 插件把自己的代码"放进"网页里干活（你不用管原理，只需知道：没注入 = 过滤不生效） |
| **刷新 / 重载扩展** | 打开 `chrome://extensions` 页面，找到时光机卡片上的圆形箭头按钮，点一下 |
| **service worker 报错** | 在 `chrome://extensions` 页面，时光机卡片上有个「Service Worker」蓝色链接，点开是黑色调试窗口，红色文字 = 出错了 |
| **console 控制台** | 在网页上按 F12 打开的开发者工具里的 Console 标签页，红色文字通常是报错 |
| **display:none** | 网页术语，意思是"彻底隐藏"。看到它就等于"帖子被藏起来了"，你肉眼看到帖子消失即是 |
| **占位条** | 折叠模式下替代帖子的那个灰色小条子 |
| **悬浮提示条 / 悬浮条** | 页面右下角出现的半透明深色小框，实时显示过滤情况（需在设置页手动打开） |
| **虚拟分页** | 个股页面上，插件自己造的一套"第 1 页 / 下一页"按钮（代替网站原本的分页），让你按时间往前翻旧帖 |
| **原生列表 / 原生分页** | "原生的" = 网站自带的（不是插件生成的）。比如雪球个股页自带的帖子列表和底部翻页条 |
| **扫描 / 已扫描 N 个原始页** | 插件为了凑齐符合时间条件的 10 条帖子，偷偷往后翻了几页网站自带列表 |
| **去重** | 同一条帖子不会被算两次、出现两次 |
| **缓存** | 点"上一页"时直接用之前看过的内容，速度很快，也不重新加载 |
| **适配包** | 一份"说明书"，告诉插件在每个网站上怎么找帖子、怎么读时间。每个网站一份 |
| **失效模态** | 当网站改版导致插件找不到帖子时，屏幕中央弹出的一个大弹窗「页面结构可能已变化」，只能点「刷新重试」 |
| **无法解析（N 条）** | 有 N 条帖子的时间插件看不懂，这些帖子默认会正常显示（不会误删） |
| **风控 / 访问验证滑块** | 雪球的反作弊机制：操作太快会弹出滑块验证码。触发后本次测试结果作废 |
| **误杀 / 漏杀** | 误杀 = 该显示的帖子被藏了；漏杀 = 该藏的帖子还在显示 |
| **跨 Tab 同步** | 开两个标签页看同一个网站，A 标签页改了时间设置，B 标签页自动跟着变 |

---

## 二、测试前的准备（一次做完）

### 准备 1：确认代码没问题（可选，开发者在 WSL 里跑）
```
npm test          → 12 个文件全部通过
npx tsc --noEmit  → 不输出任何错误
rm -rf dist && npm run build   → 构建成功
npm run test:build → 生成 dist-test 文件夹
```

### 准备 2：装到 Chrome 里
1. Windows 本机 Chrome 打开 `chrome://extensions`
2. 右上角打开「开发者模式」开关
3. 点「加载已解压的扩展程序」，选择项目的 `dist-test` 文件夹
4. ✅ 通过标准：时光机图标出现在工具栏，点开没有红色报错按钮

### 准备 3：测试纪律（很重要！）
- 用**平时已登录雪球的那个 Chrome 环境**测，东财等站点登录态下不会出滑块
- **动作要慢**：连续翻页/切换不超过 2~3 次，每次间隔至少 3 秒
- ⚠️ 一旦雪球弹出「访问验证」滑块 → **立即停止测试**，手动划过滑块后休息 5 分钟再继续。之前那一步的结果不算数
- 每次切换类别或翻页后，先等页面列表完全加载稳定，再观察结果

### 怎么记录结果
- 通过了就在对应条目打 `[x]`
- 失败就在旁边写一句现象描述（最好截图 + F12 控制台红字）

---

## 三、正式测试清单

### 第 1 组：安装和首次引导

**1.1 首次安装**
- 操作：新建一个全新 Chrome Profile（右上角头像 → 添加），在这个新环境里加载 dist-test
- ✅ 应该看到：
  - 自动弹出一个欢迎引导页，共 3 步，能顺利走完
  - 走完后，插件默认是关闭状态（图标灰色）

**1.2 更新版本时不弹引导**
- 操作：在已有 Profile 上，把 vite.config.ts 里的 version 加一点重新构建加载
- ✅ 应该看到：更新后**不会**自动弹出欢迎页（只有第一次安装才弹）

**1.3 引导结果被记住**
- 操作：走完引导后，F12 → Application（应用）→ Storage → Local Storage / 扩展存储里查看
- ✅ 应该看到：`prefs.defaultStrategy` 和 `prefs.onboarded` 这两项存在。（不懂也没关系，这项目主要给开发核对）

**1.4 重载扩展后的稳定性（已知问题）**
- 操作：重载扩展 → 新开一个雪球标签页 → 点插件图标
- ⚠️ 这是已知问题：可能不过滤。缓解办法：点一下 Popup 再刷新页面就能恢复
- ✅ 记录下现象即可；另外检查 Service Worker 调试窗口无红色报错

---

### 第 2 组：授权（允许插件访问网站）

**2.1 没授权时的样子**
- 操作：新 Profile 打开雪球 → 点插件图标
- ✅ 应该看到：
  - 弹窗里写着「此站点未授权」，有个「授权此站点」按钮
  - 过滤开关是灰的（点不动），时间输入框也是灰的

**2.2 授权流程**
- 操作：点「授权此站点」→ Chrome 弹出权限询问 → 点允许
- ✅ 应该看到：
  - 授权区域消失，时间和开关都能点了
  - 当前页面立刻开始过滤：刷新后，晚于设定时间的帖子消失，早于的保留

**2.3 带 www 的网址也能授权**
- 操作：访问 `www.jisilu.cn` → 点图标授权
- ✅ 应该看到：一次授权同时覆盖 `www.jisilu.cn` 和 `jisilu.cn`，不会陷入"反复要求授权"的死循环

**2.4 撤销授权立即停用（重点复核）**
- 操作：
  1. 先正常开启过滤，确保有帖子已被隐藏
  2. 打开插件设置页 → 「已授权站点管理」→ 移除 xueqiu.com
  3. 回到雪球标签页，**不要刷新**
- ✅ 应该看到：
  - 被隐藏/折叠的帖子**立刻全部恢复显示**，占位条消失，右下角悬浮条消失
  - 插件图标立刻变灰，角标数字清空
  - 继续滚动加载出的新帖子也**不再**被过滤
  - 刷新页面后依然没有任何过滤效果，F12 无报错

**2.5 设置页管理授权**
- ✅ 设置页能列出所有已授权网站；点「移除」后该网站从列表中消失（配合 2.4 一起测）

**2.6 无关网站不能设置时间**
- 操作：打开 github.com → 点插件图标
- ✅ 应该看到：显示「未知站点」，所有时间和预设按钮都是灰的点不动

---

### 第 3 组：时间设置面板（Popup）

**3.1 截止模式**
- 操作：点图标 → 选择「截止」模式 → 选一个日期时间（精确到分钟，**输入框失焦即自动保存**，无需点保存按钮）
- ✅ 应该看到：再打开面板，显示的还是刚才设的时间

**3.2 区间模式和纠错提示**
- 操作：切到「区间」模式 → 出现开始/结束两个输入框（输入框失焦即自动保存）
- ✅ 应该看到：
  - 开始时间设得比结束晚 → 提示「开始时间不能晚于结束时间」，且之前有效的设置没被破坏
  - 只填了一个时间就切走焦点 → 提示「请同时设置开始时间和结束时间」

**3.3 快捷预设按钮**
- ✅ 雪球页面下面板里有 6 个截止预设：「1小时前 / 今日0点 / 昨日0点 / 今天09:15 / 今天09:30 / 昨日收盘前」。逐个点击，输入框的值跟着变，保存生效
- ✅ 区间模式有 4 个预设（今天/昨天/近3天/本周），起止值正确（如"昨天"= 昨天00:00 ~ 昨天23:59）

**3.4 边界精确性（零误差抽查）**
- 操作：把截止时间 T 设成某条帖子发布的**那一分钟**，再找一条比它晚 1 分钟发的帖子对比
- ✅ 应该看到：恰好等于 T 的帖子**显示**；晚 1 分钟的被**隐藏**。随机抽 10 条核对，全部和页面显示的时间对得上

**3.5 设置能记住**
- ✅ 关掉面板、刷新页面后，截止/区间时间还是原来设的值

**3.6 双标签页同步**
- 操作：开 A、B 两个标签页都看雪球 → 在 A 的面板里改时间
- ✅ 应该看到：B 标签页**不用做任何操作**，自动按新时间重新过滤了

---

### 第 4 组：过滤效果

**4.1 隐藏策略**
- 操作：雪球首页设「1小时前」→ 开启过滤
- ✅ 应该看到：一小时内的帖子消失，更早的保留，页面布局不乱（没有大空白或错位）

**4.2 折叠策略**
- 操作：面板里把策略切成折叠
- ✅ 应该看到：
  - 被过滤的帖子变成一条「⏳ 此帖被时光机过滤」小条子
  - 点击条子 → 帖子展开、条子消失；重新设置后又恢复折叠
  - 展开后帖子的样式和网站原生的看起来一模一样，没有变形跳动

**4.3 关闭再开启**
- ✅ 关闭过滤 → 所有帖子（包括展开状态的）全部恢复；再开启 → 又被过滤；计数归零后重新累计

**4.4 看不懂时间的帖子**
- ✅ 个别时间格式特殊的帖子会正常显示（不被误藏）；面板和悬浮条上标注「N 条无法解析」

**4.5 计数和角标**
- ✅ 面板上「已过滤 N 条」和图标上的小数字随滚动实时增加（几乎瞬间到位）
- ✅ 改时间或关过滤后，计数清零重来

**4.6 右下角悬浮条**
- 操作：设置页打开悬浮条开关 → 回到雪球页面滚动
- ✅ 应该看到：
  - 右下角出现深色半透明小框，内容含边界时间和已过滤数量
  - 滚动加载时数字平稳更新，不闪烁不抖动
  - 点 × 能关掉；设置页关闭开关后，新页面也不再出现

---

### 第 5 组：雪球首页各种分类

**5.1 顶部大类切换**
- 操作：登录态首页，逐个切换：关注 / 热门 / 7x24 / 自选 / 视频 / 基金 / 资讯 / 达人 / 私募 / ETF
- ✅ 每一类下过滤都正常生效；切换很干脆，没有上一个分类的内容残留混进来；每个分类的计数从自己算起（互不污染）

**5.2 二级分类**
- ✅ 关注流下的「全部/关注精选/只看原发/未分组」、自选流下的「全部/公告/新闻」各自独立过滤正常

**5.3 7x24 快讯**
- ✅ 快讯区按「今天」「昨天」分组标题 + 每条的 HH:mm 时间综合判断：今天的快讯按截止时间隐藏/保留，昨天的同理

**5.4 智能排序类的诚实提示**
- ✅ 在热门等"智能流"分类下，面板会说明「当前类别为智能或热度信息流，仅过滤已加载内容」（因为这类排序没法保证翻遍全部历史）；悬浮条也有同样提示

**5.5 连续往下滚**
- 操作：同一分类连续向下滚动约 3 屏
- ✅ 没有重复帖子、计数不错乱、没有别分类的内容串进来

---

### 第 6 组：雪球个股页"虚拟翻页"（最复杂的一组，动作放慢！）

> 场景说明：打开某只股票的讨论页，插件会用自己的一套翻页控件接管，帮你从符合时间条件的帖子里一页页（每页10条）往回找。

**6.1 六个分类逐个验**
- 操作：依次切换 全部 / 讨论 / 投资者关系 / 交易 / 资讯 / 公告
- ✅ 每类下：虚拟第 1 页恰好 10 条、上方显示「第 1 页 · 已扫描 N 个原始页」、网站自带的列表和底部分页条都被藏起来了

**6.2 第一屏**
- ✅ 第一屏从网站第 1 页开始往后找，凑满 10 条符合时间的帖子就停（不多翻页）。就算截止时间设得很狠导致第一页全是空档，也会自动跨页凑出内容，并显示扫了几页——不会给你一片空白

**6.3 点"下一页"**
- ✅ 只有点了「下一页」才继续向后扫描；第 2 页和第 1 页没有重复帖子

**6.4 点"上一页"**
- ✅ 秒出（用的缓存），扫描页数不变、网络里也没有新的请求

**6.5 各种尽头提示**
- ✅ 翻到底显示「已到末页」；扫满 200 页显示「已达上限」；网站加载失败显示「原始页面加载失败」

**6.6 中途取消**
- 操作：扫描进行中点「取消扫描」
- ✅ 显示「扫描已取消」，已经收集到的虚拟页保留可用

**6.7 扫描中途改变主意**
- ✅ 扫描中改截止时间 → 旧扫描停止，按新时间重新开始
- ✅ 扫描中关闭过滤 → 插件的翻页控件消失，网站原生列表和分页条恢复显示

**6.8 空分类**
- ✅ 「投资者关系」如果本来没内容，保留网站原生的「该股票暂无信息」，不出现插件的翻页控件，也无报错；切回「讨论」后一切恢复正常

**6.9 分类来回切**
- ✅ 六个分类之间来回切换：每次都是新分类从第 1 页重新开始，各分类缓存互不影响

**6.10 和网站"抢回"隐藏权**
- （此项较难手动观察，可跳过）网站有时会把隐藏的列表又显示出来，插件会在 1 秒内重新藏好

---

### 第 7 组：帖子详情页的评论过滤

**7.1 评论按自己的时间判断**
- ✅ 一条老帖子下面的新评论，会被单独隐藏（评论按评论自己的发布时间算，不看帖子时间）

**7.2 「今天/昨天 HH:mm」这种写法的评论**
- 操作：找一条显示为「昨天 16:23 · 江苏」的评论，把截止时间分别设在它之前/之后试
- ✅ 截止在其后 → 评论保留；截止在其前 → 评论隐藏（按具体时刻判断，误差不超过几分钟）

**7.3 没有时间的评论**
- 操作：设置页找到「评论回退策略」
- ✅ 策略=全部显示：没时间的评论正常显示，计入「无法解析」数
- ✅ 策略=默认折叠：没时间的评论变占位条
- ✅ 切换策略立即生效

**7.4 区间模式下的评论**
- ✅ 区间内的评论保留，区间外的（包括标着「今天」的）隐藏

---

### 第 8 组：快捷键

- ✅ 按 `Ctrl+Shift+T` → 当前页面的过滤开关切换，图标彩色↔灰色跟着变
- ✅ 在没设时间/没授权的页面上按快捷键 → 没有任何反应，图标不变

---

### 第 9 组：网站改版保护和诊断

**9.1 网站结构变了要报警**
- 操作：（此项需要开发配合篡改配置让选择器失效）刷新首页
- ✅ 5 秒内屏幕中央弹出不可关闭的大弹窗「页面结构可能已变化」，带「刷新重试」按钮

**9.2 个人主页和搜索页不该误报**
- 操作：访问某用户的雪球主页、搜索结果页，等 6 秒
- ✅ 不弹任何警告弹窗，页面完全不受影响

**9.3 详情页也不该误报**
- 操作：打开一篇帖子详情页（带正文+评论的），等 6 秒
- ✅ 不弹失效警告

**9.4 看诊断明细**
- ✅ 面板里展开「查看无法解析的时间」，能看到几条（≤8 条）样本，包含原始时间文本

**9.5 诊断报告不泄露隐私**
- 操作：点「复制诊断报告」，粘贴到记事本看
- ✅ 内容只有：平台名、适配包版本、过滤模式和时间边界、各项计数、页面路径和时间原文
- ❌ 不应该出现：完整网址参数、帖子正文内容、你的账号信息、Cookie

---

### 第 10 组：其他网站

**10.1 同花顺 t.10jqka.com.cn**
- ✅ 授权后首页信息流按「月日 + 时:分」组合时间过滤正常
- ✅ 去年的老帖（只有月日没年份）被正确当作去年处理；今年的帖子补今年
- ✅ 个股吧等其他页面插件安静地不工作，无报错

**10.2 集思录 jisilu.cn**
- ✅ 分类列表页按「YYYY-MM-DD HH:mm」格式的时间过滤
- ✅ 帖子详情页里的回复按各自时间过滤
- 📝 观察记录：大热帖的回复翻页是怎么加载的（供后续改进）
- ✅ 首页 explore 这种没有时间的地方，插件静默退出，不弹警告

**10.3 东方财富资讯 finance.eastmoney.com**
- ✅ 「czqyw」「cgspl」两个栏目列表按「2026年08月17日 20:16」这种中文格式过滤
- ✅ 底部 `_2.html`、`_3.html` 这样的翻页每一页都能过滤
- ✅ 正文页（单篇文章页）插件静默退出

---

### 第 11 组：适配包在线更新机制

> 说明：真正的更新服务器还没搭，这里只测"机制本身会不会出错"。

- ✅ 装好后会尝试联网拉取一次；连不上/404 时安静地继续用内置版本（Service Worker 日志可见，界面上无任何报错）
- ✅ 设置页关掉「适配包自动更新」开关后不再发起请求

---

### 第 12 组：流畅度测试

- 操作：雪球首页有 50~80 条帖子，连续滚动 3 屏；想专业一点可以按 F12 → Performance 录制
- ✅ 滚动无明显卡顿
- ✅ 悬浮条开着时滚动也不额外卡
- ✅ 个股页扫描进行中，页面依然可以正常点击、滚动（没有被卡死的感觉）

---

### 第 13 组：全新环境全流程演练（发布前必做）

- 操作：全新 Chrome Profile → 加载打包好的 ZIP → 完整走一遍
- 流程：引导 3 步 → 默认关闭 → 雪球授权 → 设时间 → 过滤生效 → 关闭恢复 → 评论过滤 → 失效警告弹窗
- ✅ 全程无报错
- ✅ 按 F12 → Network 标签页检查：所有网络请求只指向 jsDelivr 或 GitHub，没有任何其他第三方网站（隐私承诺）

---

### 第 14 组：上架前准备核对

- ✅ `npm run package` 生成的 ZIP 里不含开发密钥
- ✅ manifest 版本号是 1.0.0
- ✅ 商店描述、关键词、分类填好了
- ✅ 隐私政策有一个公开可访问的网址
- ✅ 截图 5~6 张（引导页、虚拟翻页、隐藏/折叠效果、设置页、同花顺）
- ✅ 本清单全部打勾，失败项都已修复重测

---

## 四、测试结果记录表（抄自 DEV-PLAN 附录 C）

| 项 | 内容 |
|---|---|
| 通过项 | ________ |
| 失败项（附截图/console 红字） | ________ |
| 结论 | □ 可进入发布　□ 需修复后重测 |

## 五、历史已通过项（不必重测，除非怀疑退化）

| 日期 | 内容 | 结果 |
|------|------|------|
| 08-13 | 雪球基础截止过滤（早期构建） | ✅ 用户确认 |
| 08-17 | 详情评论区间过滤双向样本 | ✅ |
| 08-17 | 首页普通类别、7x24、个股翻页基本功能 | ✅（建议最新构建复验） |
| 08-17 | 「交易」类别测试触发了风控滑块，结果无效 | ⚠️ 见第 6 组 6.1，需低频重测 |
